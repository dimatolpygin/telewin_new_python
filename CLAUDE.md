# tgbot_py — Python-порт бота ИИ-поиска по прайсу «Домашний мастер»

Рабочий Python-порт бота (@NikaAi1Bot) — основной артефакт стенда `telewin_tests`.
Поиск = лексика (словарь+атрибуты+фаззи) ⊕ вектор (pgvector) через RRF.

**Отдельный git-репо**, remote = **публичный** `github.com/dimatolpygin/telewin_new_python` (с этапа 25;
ветка `dev` + теги запушены). Push требует интерактивной GitHub-авторизации через Git Credential
Manager (кэш-токена нет) — при `git push` откроется браузер. Ведётся по okplan: `docs/STATUS.md`
(снимок, читать первым), `docs/ROADMAP.md` (критерии), теги `stage-N-done`.

- **Milestone 1 (этапы 0–10) закрыт** — гибридный поиск работает.
- **Milestone 2 «Апгрейд качества поиска» (этапы 11–16) — ЗАВЕРШЁН** (теги `stage-11..16-done`):
  11 (линейка+baseline), 12 (абстейн-гейт 34%→91%), 13 (словарь народных названий/кодов), 14
  (атрибуты дюймы/цоколь/размеры), 15 (бренды: чистка стран + буст + честная пометка), 16 (канал
  родовых действий «чем красить»→ЛКМ + уточнение). Итоговая сводка по 5 сегментам — `docs/ZAMER_RODOVYE.md`
  (все цели достигнуты). Находки прочёса — `docs/NAHODKI_PRAJSA.md`, baseline — `docs/BASELINE_V2.md`.
- **Milestone 3 «Ежедневное обновление данных из БД» (этапы 19–23) — ЗАВЕРШЁН**: 19 (UPSERT-diff),
  20 (инкрем. re-embed + дата актуальности), 21 (монитор новизны), 22 (оркестрация `run.py`),
  23 (трекер `novelty_families`). Модули — `bot/update/`.
- **Milestone 4 «Автообновление из FTP по расписанию» (этапы 17, 18, 24) — ЗАВЕРШЁН**: 17 (FTP-приём
  `istochnik.py`), 18 (автораскладка 8↔11 `parser.py`), 24 (планировщик `scripts/update.sh` +
  `deploy/*.{service,timer}` + runbook `docs/DEPLOY_UPDATE.md`). Цепочка «файл на FTP → подхватился
  сам → бот по свежему каталогу» замкнута в коде, переносимо под Ubuntu.
- **Milestone 5 «Продакшн: мультиканал + деплой» (этапы 25–31) — ЗАВЕРШЁН** (теги `stage-25..31-done`):
  25 (каналонезависимое ядро `bot/core.py` + публичный репо), 26 (логи «каждая щель» + request-id),
  27 (канал VK), 28 (канал MAX), 29 (оркестрация 3 каналов), 30 (Docker-стек), 31 (прод-деплой +
  CI/CD). **Бот в ПРОДЕ на `87.58.210.46`** (`/opt/telewin/tgbot_py`, ветка `master`): Docker-стек
  app(3 канала)+pgvector+redis, автодеплой `master`→GitHub Actions, автообновление прайса FTP+cron 04:00.
  Векторы ПЕРЕНЕСЕНЫ дампом (не пересчитаны). Локальный поллер держать остановленным (конфликт токена
  `@NikaAi1Bot`). Секреты — GitHub Secrets + `.env` на сервере. **Проект закрыт — все этапы 0–31.**

## Неочевидное (без этого сломаешь или будешь искать час)

- **БД бота — отдельный pgvector-контейнер `telewin-test-pg` на :5434**, НЕ общий `postgres16`
  (:5432). `.env` → `PGPORT=5434`. Общий `postgres16` БЕЗ pgvector и делит хост с чужими
  проектами (`maria_doll`, `sbavito`, `landing_bot`, `hermes_agent`) — **его образ не менять**.
  Данные в volume `telewin_test_pgdata` (переживает пересоздание контейнера).
- Столбец эмбеддингов — **`embedding halfvec(3072)`**, не `vector`: HNSW по `vector` держит
  ≤2000 измерений, а gemini даёт 3072. Индекс `products_embedding_hnsw` (halfvec_cosine_ops).
- Эмбеддинги — OpenRouter `/embeddings` (`google/gemini-embedding-001`, 3072), нужен
  `OPENROUTER_API_KEY`. Пересчёт всех 11 864: `python -m bot.embed_index --all` — **~35 мин**
  (OpenRouter троттлит) и стоит денег. Текст = обогащённое имя (`bot/search/obogashenie.py`).
- **RRF-гаджет `bot/search/gibrid.py`: `W_VEC=0.3` — не поднимать.** При >0.3 вектор бьёт точный
  лексический матч и золотой набор регрессирует 38→37 («саморез черный 45»→ШУЦ). Порог 0.76
  держит абстейн на мусоре. Жёсткие каналы (штрихкод/артикул/производитель) короткозамыкают.
- Замер гибрида: `python -m bot.search.zamer_gibrid` (нужны посчитанные эмбеддинги).
  Результаты: слепой 85% top-1, золото 95% без регресса — `docs/ZAMER_GIBRID.md`.
- Слепой набор `data/test_nabor.json` — 302 записи; `ждём` = регэксп по имени товара,
  `ждём=null` = мусор (успех = абстейн). При росте набора валидировать регэксп против прайса.
- LLM-агент местами консервативен (уточняет до показа, «пассатижи»≠«плоскогубцы») — это промт
  этапов 3/6 (`bot/ai/agent.py`), НЕ поиск; тюнить отдельной задачей.
- **Слабое звено — агентский слой, НЕ поиск.** Баг «масло пихтовое: то нет, то есть» — LLM при
  `tool_choice=auto` относил реальный товар к «непрофильному» и отвечал «не найдено» БЕЗ вызова
  поиска. Фикс (коммит `af94e1e`): предохранитель в `run_agent` — на отказ без поиска форсирует
  повтор с `tool_choice=required` (+ `_zayavil_otkaz`, промт-запрет). **Не убирать** — иначе баг
  вернётся. Сам поиск детерминирован; «не найдено» без пустой выдачи инструмента = ошибка агента.
- **Абстейн-гейт (этап 12) — `_chuzhoy_domen` в `bot/search/gibrid.py` + `data/chuzhoy_domen.json`.**
  На запрос из ЧУЖОГО домена (косметика/еда/техника/одежда/зоо) гибрид отвечает `[], "чужой домен"`
  ДО всех каналов (утечки шли и через лексику `словарь+атрибуты`, не только вектор — порогом 0.76
  не закрывалось). Абстейн 34%→91%. **Инвариант при правке словаря: 0 ложных срабатываний на
  ЗОЛОТОМ+СЛЕПОМ товарных** (иначе режется recall) — прогонять `baseline_v2` после каждой правки.
  Приборы-омонимы («масляный обогреватель», «мясорубка электрич») — ФРАЗАМИ, не голым словом
  (иначе гейтит стоковый плёночный обогреватель / втулку мясорубки). Реальный ассортимент (мыло,
  белизна, масло пихтовое, репеллент) маркеров НЕ содержит и не гейтится — проверять при добавлении.
- **Поле `proizvoditel` загрязнено СТРАНАМИ**: «Россия» 4411, «Китай» 360, «Без бренда» 304,
  «ноль» 61 (+Германия/КНР) — не бренды. **Этап 15 (`data/brendy.json → не_бренд`)** исключил их
  из стем-индекса канала производителя (был баг «россия»→4411 товаров). Уточнение к прежней заметке:
  народные марки инструмента в новом полном прайсе ЕСТЬ, но лишь как аксессуары (нож к рубанку
  MAKITA, пилки BOSCH, буры METABO) — самих электроинструментов этих брендов нет. Поэтому «честный
  отказ» динамический: `Poisk.brend_iz_zaprosa` + пометка `примечание_бренд` в агенте срабатывают,
  когда бренд назван, но НИ ОДНА найденная позиция ему не соответствует («болгарка макита»→УШМ-аналоги
  + «Makita нет»). Буст бренда +3 внутри семейства («болгарка вихрь»→УШМ Вихрь) — с целевой дотяжкой
  брендовых семейств, т.к. УШМ дробится по модели. Мерочные наборы бренды не называют → регресса нет.
- **Буст токена `слово_в_имени` = 4.0** (этап 13, `search.py`, было 3.0). Это «пользователь назвал
  подтип» (филипс→PH, гипсокартон→ШСГД, масляная→ПФ-115) — должен бить общее слово. Не опускать:
  при 3.0 «масляная **краска**» уходит к семейству Краска (водоэмульсионки/Серебрянка) поверх Эмаль
  ПФ-115. **В `слово_в_имени` класть только слова-ПОДТИПЫ, не прилагательные-определения:** токен
  `светодиодный→светодиод` пришлось убрать — он бустил ЛЮБУЮ строку со словом «светодиод», и родовой
  «светильник светодиодный» уходил в Модуль вместо Светильника (регресс 89→87). «Светодиодная лампа»
  как следствие отдаёт лампу накаливания — контекстный выбор отложен на этап 16.
- **Дюймы `1/2` vs `1½` (15мм vs 40мм) — ЗАКРЫТО в этапе 14** (`attributes.py`): прайс пишет
  полтора как `1 1/2"` или `1" 1/2 (40)` (НЕ символом ½), целое ПЕРЕД дробью → атрибут «1 1/2»≠«1/2».
  Там же добавлены атрибуты: **цоколь** (E27/E14/E40 + GU10/GX53/G9/G4/G23/T8, нормировка Е→E),
  **цв_температура** (2700/4000/6500К + тёплый/нейтральный/холодный), **ток_а** (16/25/40А автоматов).
  **Цоколь из запроса матчим по `q.lower()`, НЕ по `norm`** — norm-гомоглиф латинскую `x→х` ломает «gx53».
  **Цв.температура (2700/4000/6500) при лампа-контексте НЕ идёт в артикул-канал** (4 цифры иначе
  короткозамыкали на товар с таким артикулом). Остаточно путаются точные размеры крепежа (М12→М10 в
  части кейсов, 4.8→4.2, уголок «40*40*80» где 3-е число = высота ребра) — размерная точность 91%.
- Инструменты UAT/аналитики: `python -m bot.search.probe_batch <файл_запросов>` (произвольные
  запросы через гибрид → JSON, для регрессии), `python -m bot.search.dump_group "<группа>"`
  (выгрузка среза прайса), **`python -m bot.search.baseline_v2 [--save]`** — baseline milestone 2
  по 5 сегментам (абстейн-точность / top-1 / hit@5 / размерная точн.) + контроль золота. ГЛАВНАЯ
  мерка апгрейда: прогонять после КАЖДОЙ правки словаря/поиска. `--save` пишет `docs/BASELINE_V2.md`
  (**это замороженный baseline этапа 11 — не перезаписывать бездумно**; результаты правок класть в
  отдельный `docs/ZAMER_*.md`, как `ZAMER_ABSTAIN.md` для этапа 12). Наборы: `data/abstain_nabor.json`
  (44 чужих, `ждём=null`), `data/razmer_nabor.json` (32 размерных с «ждём»+«анти»). Сегментация
  слепого — по полю `прием` (мусор/сленг/опечатка/родовое/размер). Текущий baseline: абстейн 91%,
  разговорный 83%, родовой 89%, размер 84%, размерный-точный 84%; золото 95%.

## Неочевидное — обновление прайса (Milestone 3)

- **Две РАЗНЫЕ заливки прайса — не путать.** `bot/import_price.py` = первичная с нуля
  (`DROP`+`COPY`, затем `embed_index --all` ~35 мин, деньги). `bot/update/sync.py` = ежедневный
  ИНКРЕМЕНТ (UPSERT, эмбеддинги не трогает). Запустишь `import_price` на обновление — сотрёшь
  колонку `embedding` у всех 11 864 и попадёшь на полный re-embed. **Для апдейта — только `sync`.**
- **Ключ апдейта — ШТРИХКОД, не артикул.** Артикул Фора переиспользует (200 дублей, 188 с РАЗНЫМИ
  именами: код `00135` = и саморез ШСГД, и вилы навозные; +215 пустых). Штрихкод 100% заполнен и
  уникален (вкл. внутренние коды `2000…`). Сменишь ключ на артикул — diff разъедется.
- **`sync` сам НЕ считает векторы и НЕ мониторит** — нужны флаги: `--apply-embed` (досчёт ТОЛЬКО
  новым/переименованным, режим `where embedding is null`) и `--monitor` (новизна → `docs/NOVELTY.md`).
  Без `--apply-embed` новинки лягут с `embedding NULL`: ищутся по имени/штрихкоду, вектором — нет.
  Смена только цены/остатка вектор НЕ трогает (embedding = f(обогащённое имя), не цены).
- **Дата прайса больше НЕ хардкод.** `ai/agent.py::ДАТА_ПРАЙСА` — лишь fallback; реальная из таблицы
  `price_meta` (пишет `sync`, парс из имени файла `…YYYY-MM-DDThh-mm-ss…`/mtime) через
  `run_agent(data_prajsa=…)`. `main.py` читает её на КАЖДЫЙ запрос (свежесть без перезапуска).
- **Новые таблицы в схеме кроме `products`:** `price_meta` (дата актуальности + `file_name`, синглтон
  id=1), `known_podgruppy` (базлайн подгрупп для монитора; ПЕРВЫЙ прогон = посев, не сигналит),
  `novelty_families` (трекер новых семейств, этап 23: `status` new/внесено, авто-закрытие по словарю).
- **Тестовый фикстур апдейта:** `data/make_test_price.py` → `samples/ТЕСТ_2026-07-19…xls` (2373 цены
  +5%, 3 новинки: Лампа=знакомое семейство молчит, Пирометр/Гигрометр=новые сигналят) +
  `data/test_price_manifest.json`. **Откат стенда в чистое 18.07:** `python -m bot.update.sync
  <боевой 2026-07-18T11-29-30.xls>` (вернёт цены, удалит новинки; проверка целостности — MD5-хеш
  колонки `embedding` до/после совпадает).

## Неочевидное — автообновление FTP/расписание (Milestone 4)

- **FTP-креды НЕ в этом репо.** Пароль лежит в `.env` (gitignore) — заполняется вручную из соседнего
  проекта `Downloads/ЗАКАЗЫ/telewin/.env` / `docs/ACCESSES.md` (хост `87.58.210.46:21`, логин
  `foraprice`, папка `price`, ПАССИВНЫЙ режим, диапазон 40000-40100). В git/CLAUDE.md пароль не класть.
  Пусто `PRICE_FTP_HOST` → `--ftp` даёт понятную ошибку, работает только локальный `PRICE_XLS`.
- **`--skip-known` сравнивает по ИМЕНИ файла** (`price_meta.file_name`), не по содержимому: тот же
  файл под другим именем НЕ пропустится, а переупакованный под тем же именем — пропустится. Проверка
  БЕЗ скачивания (`istochnik.svezhee_imya`). Цепочка планировщика: `scripts/update.sh` → `run --ftp
  --skip-known`.
- **`.gitattributes` держит `*.sh`/`*.service`/`*.timer` в LF — НЕ трогать.** На Windows-разработке
  CRLF сломает bash на Ubuntu (падение на `\r`). Лок планировщика — атомарный `mkdir .update.lock.d`
  (переносимее `flock`); аварийное падение может оставить каталог-лок — снять руками (см. runbook).
- **Парсер (`parser.py`) определяет ШТРИХКОД по СОДЕРЖИМОМУ** (доля длинных числовых в col0/col1),
  т.к. в образце 06.07 артикул↔штрихкод стояли наоборот. Раскладка — по числу колонок (11/8, иначе
  `ParserError` ДО касания БД). **У 8-кол штрихкода нет** → ключ апдейта падает на `(артикул, имя)`;
  СМЕНА формата 11→8 на боевом = массовый insert/delete (ключ у всех меняется) — если Фора вернёт
  8-кол, это осознанно ловить, не пугаться. `validate.py` ослаблен до ≥8 колонок (раскладку решает parser).
- **Прод-развёртывание НЕ сделано.** `deploy/*.{service,timer}` — с путями-плейсхолдерами
  (`/opt/telewin/tgbot_py`), их править под сервер; запуск на Ubuntu — по `docs/DEPLOY_UPDATE.md`.

## Неочевидное — мультиканал/Docker/деплой (Milestone 5)

- **Ядро каналонезависимо.** `bot/core.py::Yadro.obrabotat(channel, chat_key, user_text, typing_cb=)`
  — вся доменная логика (замок на `(канал, чат)`, дата прайса, `run_agent`, сессия). Адаптеры
  `bot/channels/{telegram,vk,max}.py` — тонкий транспорт. `bot/main.py` = `sozdat_yadro`
  (`bot/bootstrap.py`, индекс поиска ОДИН раз) + `asyncio.gather` под супервизором `_supervise`
  (падение канала логируется+перезапуск, соседи живы). **Добавить канал = адаптер + строка в `KANALY`.**
- **Сессии разведены по каналам:** ключ Redis `telewin:dialog:{канал}:{chat}` (`bot/session.py`) —
  VK peer_id и TG chat_id с одним числом НЕ схлопываются. **Канал выключается пустым токеном** в `.env`
  (адаптер возвращается сразу): `BOT_TOKEN`/`VK_TOKEN`+`VK_GROUP_ID`/`MAX_TOKEN`.
- **request-id — через `contextvars` в `bot/logger.py`.** Адаптер оборачивает обработку в
  `with nachat_zapros(CHANNEL):` — фильтр подмешивает `[канал rid]` в КАЖДУЮ строку без проброса
  параметром. `LOG_LEVEL=debug` добавляет промпт/ответ ИИ (на `info` — нет). Помощники
  `log_vhodyashchee/tool_call/vyzov_ii/ishodyashchee/oshibka`.
- **MAX — самое неочевидное (сожжёшь час):** база **`https://platform-api2.max.ru`** (не старый
  `botapi`/`platform-api`), токен **заголовком `Authorization: <токен>`** (query устарел). **TLS под
  сертификатом Минцифры** — стандартного certifi мало, `bot/channels/max.py::_ssl_context` строит
  bundle certifi ⊕ `certs/russian_trusted_{root,sub}_ca.cer` (публичные CA в репо — не удалять!).
  Long Poll: `GET /updates?marker=&timeout=` → `{updates, marker}`; текст в `message.body.text`,
  chat_id в `message.recipient.chat_id`. VK — raw Bots Long Poll (без `vkbottle`), `failed:1/2/3`.
- **`import_price` теперь самодостаточен** (этап 30): заводит `create extension vector` + столбец
  `embedding halfvec(3072)` В КОДЕ (раньше вручную через psql → деплой был невоспроизводим). Починен
  латентный баг: COPY писал NULL в `serial id` (в `products.json` поля `id` нет) → `id` исключён из
  COPY, serial генерит сам. На чистом volume всплывало, на обжитой БД маскировалось.
- **Docker-стек** (`docker-compose.yml`): `db` (pgvector/pgvector:pg16, initdb `docker/initdb/*.sql`
  → extension+схема), `redis`, `app` (`env_file .env`, PGHOST/REDIS_URL переопределены на сервисы
  compose), `updater` (profile `tools`, одноразовый цикл для host-cron). Init-порядок и запуск —
  `docs/DEPLOY_DOCKER.md`. Два инстанса на одном `BOT_TOKEN` → `TelegramConflictError` (Telegram
  отдаёт getUpdates одному) — держать что-то одно.
- **ПРОД ЖИВОЙ (этап 31): `87.58.210.46`, `/opt/telewin/tgbot_py`, ветка `master`.** Docker-стек
  app(3 канала)+pgvector+redis. SSH `root@87.58.210.46:22` (пароль/доступы — в приватном
  `Downloads/ЗАКАЗЫ/telewin/docs/ACCESSES.md`, НЕ в этом репо; deploy-ключ ed25519 в `.secrets/`).
  Логи: `docker compose logs -f app`. `vsftpd` (FTP-приём прайса :21) живёт на том же хосте рядом.
- **Векторы на прод ПЕРЕНЕСЕНЫ дампом, не пересчитаны:** `pg_dump -Fc -t telewin_test.products`
  (~154 МБ, 11864 с `halfvec(3072)`+HNSW) → `pg_restore` после `CREATE EXTENSION vector` (initdb). На
  проде `import_price`/`embed_index --all` НЕ нужны (0 обращений к OpenRouter). **NB: дамп содержит
  ТОЛЬКО `products`** — `price_meta`/`known_podgruppy`/`novelty_families` создаёт первый прогон
  `updater` (не пугаться их отсутствия сразу после restore).
- **CI/CD: push `master` → GitHub Actions → SSH → `git reset --hard origin/master` + `docker compose
  up -d --build`** (`.github/workflows/deploy.yml`, appleboy/ssh-action). Том `pgdata` (векторы)
  переживает деплой; untracked `.env` на сервере `git reset` НЕ трогает. Secrets репо:
  `SERVER_HOST`/`SERVER_USER`/`SSH_PRIVATE_KEY` (+опц. `SERVER_PORT`/`INSTALL_DIR`). **В `master`
  пушить ТОЛЬКО по явному указанию заказчика** — это выкат на прод. `dev` обычно впереди `master` на
  docs-коммиты, это норма.
- **Грабли прода (иначе потеряешь час):** (1) **держать ОДИН поллер на токен** — локальный
  `docker compose up`/`python -m bot.main` при живом проде даст `TelegramConflictError`/двойные ответы
  VK/MAX; перед локальным запуском останови прод-app или используй другой токен. (2) **`docs`/`logs` на
  сервере — `chown 1000:1000`**: контейнер под non-root `app` (uid 1000) иначе не может писать отчёты
  монитора (`PermissionError` на `docs/NOVELTY.md`). **Применено на проде 2026-07-22** (`chown -R
  1000:1000 docs logs`) — до этого ночной updater падал в самом конце на записи `NOVELTY.md` (данные
  при этом ложились, т.к. все записи в БД — до шага монитора; падал только отчёт). (3) FTP из контейнера
  к тому же хосту (`PRICE_FTP_HOST=87.58.210.46`) работает через hairpin NAT — проверено. (4) host-cron
  `*/30 * * * * … ./scripts/update_docker.sh` — поллер обновления прайса + условный рестарт бота (этап 33;
  раньше был `0 4 * * * docker compose run --rm updater` без рестарта). (5) root-пароль
  слал клиент в чате и НЕ менян — TODO: сменить/отключить парольный вход (ключ уже стоит).
- **Автообновление + рестарт бота (этап 33, РЕШЕНО).** Индекс поиска (`tovary`/`Poisk`) грузится в память
  ОДИН раз при старте процесса (`bootstrap.py`); вектор-канал отдаёт только `(id, sim)`, товар резолвится
  из памяти по id (`gibrid.py::по_id`, id вне памяти отсеиваются) → после `updater` живой `app` держал
  СТАРЫЙ каталог до рестарта/деплоя. **Фикс:** прод-cron `*/30 * * * * … ./scripts/update_docker.sh` —
  поллер гоняет `updater` и УСЛОВНО рестартит `app`. Сигнал: `run.py::_main` выходит **кодом 10**, когда
  применён новый прайс с реальными изменениями (insert/rename/upd_fact/delete>0); `update_docker.sh` на
  код 10 делает `docker compose restart app`, иначе (skip-known/0/ошибка) — нет. Проверено на проде
  2026-07-22 (пихтовое 255/442 виден боту без ручного рестарта; холостой прогон не рестартит).
  **Две грабли, вскрытые живым тестом (тоже решены):**
  (а) `docs/NOVELTY.md`/`UPDATE_RUN.md` ОТСЛЕЖИВАЛИСЬ в git → `deploy` (`git reset --hard`) клал их
  root-owned → контейнер (uid 1000) не мог перезаписать → `PermissionError` в мониторе ПОСЛЕ upsert'а
  ломал код возврата (1 вместо 10) и рестарт не срабатывал при уже изменившейся БД. Фикс: запись отчётов
  best-effort (`try/except OSError`→warning, `monitor.py`/`run.py`) + отчёты убраны из git и в `.gitignore`
  (генерятся контейнером). **Не возвращать отчёты в git.**
  (б) `updater` (profile `tools`) собирал ОТДЕЛЬНЫЙ образ, а `deploy` (`up -d --build`) профильные сервисы
  НЕ пересобирает → updater уезжал на старый код. Фикс: `app` и `updater` делят один образ
  **`image: telewin-bot:latest`** (у app `build:`+`image:`, у updater только `image:`), сборка app при
  деплое обновляет и updater. **Не давать updater отдельный `build:`.**

## Неочевидное — стиль общения (Milestone 6, этап 32)

- **Стиль ответа = промт + постобработка, РАЗДЕЛЕНО.** Промт (`bot/ai/agent.py::SYSTEM`) задаёт тон:
  персона живого консультанта **Сергей**, блок «КАК ТЫ ОБЩАЕШЬСЯ» (короткие реплики, без markdown/
  списков/канцелярита, приветствие один раз, не вываливать прайс, рекомендовать ОДНУ позицию по сути),
  антипример робо-выгрузки. **Чистоту «—»/markdown гарантирует КОД, не промт** —
  `bot/ai/stil.py::ochistit_otvet` (regex, идемпотентна) применяется к ответу в `run_agent` перед
  возвратом (модель всё равно иногда проставит `**`/«—» вопреки промту). Правило заказчика: **тире и
  markdown убирать постобработкой, не промтом.** Имя «Сергей» меняется одной строкой в `SYSTEM`.
- **Что НЕ трогать при правках стиля:** `bot/search/*`, гибрид, TOOLS/tool-calling, `brend_iz_zaprosa`/
  `примечание_бренд`, предохранитель `_zayavil_otkaz`. Стиль на ранжирование не влияет → `baseline_v2`
  прогонять не нужно (тракт поиска заморожен). Дробление ответа на несколько реплик с паузами набора
  (полный «живой чат», как референс `Downloads/vk0043/humanize.py`) в скоуп 32 НЕ входит — транспорт,
  отложено. Убрана строка промта «Всегда добавляй, что данные актуальны на {ДАТА}» — дата теперь по запросу.

## На будущее — два магазина (решение заказчика в процессе)

- Клиент думает, объединять ли базы двух магазинов или слать двумя копиями. **Сейчас прайс — ОДИН файл
  с обоими остатками в колонках** (Микро=col7, Берёз=col8); бот уже отвечает по обеим точкам. Ключ
  склейки — **штрихкод** (одинаковый товар из разных источников = одна карточка, дубли не ломают).
- **Если пойдут двумя раздельными файлами** — нужна доработка ТОЛЬКО загрузчика (`bot/update/*`), не
  поиска: пометить файл магазином, склеить по штрихкоду в одну строку (остаток в колонку своего
  магазина), **DELETE считать по магазину отдельно** (нет в файле А → обнулить остаток А, а не удалять
  строку, если товар ещё у Б; удалять только когда нет НИ У КОГО). Сейчас `sync.py` считает один файл
  ПОЛНЫМ каталогом (`to_delete = ключи БД, которых нет в файле`) → два раздельных файла в лоб дадут
  взаимное удаление. Оформить отдельным этапом, когда клиент определится.

## Прочее

- Прайс: ПОЛНЫЙ 11-колоночный файл `2026-07-18T11-29-30.xls` (11 864 строки, штрихкод 99%,
  11 групп / 69 подгрупп). Раскладка (0-based): 0 артикул, 1 штрихкод(EAN), 2 наименование,
  3 единица, 4 производитель, 5 цена, 6 остаток общий, 7 Микро, 8 Берёзовская, 9 группа, 10 подгруппа.
- FTP отдаёт пустышки 51 200 Б вместо ~2.4–3.2 МБ — уже ловится: размер-гейт `SIZE` в `istochnik.py`
  (до скачивания) + `validate.MIN_RAZMER=500 000` (после). Порог не опускать ниже пустышки.
- Стенд НЕ коммитить в родительский `C:/Users/GigaChat` (чужой remote `restoran326_bot`).
  Этот репо (`tgbot_py/`) изолирован — коммиты только сюда.
