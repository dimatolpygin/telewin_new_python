# Дорожная карта — Python-порт бота @NikaAi1Bot (ИИ-поиск по прайсу «Домашний мастер»)

11 этапов: 0–6 — от пустого каркаса до протестированного бота (закрыты), 7–10 — «v2 поиска»
(добивание качества по итогам этапа 6). Каждый этап заканчивается **демонстрируемым
результатом** (что можно потрогать руками).

Ключевая идея: **MVP-first**. Этап 1 = живой поиск из командной строки (уже ценность),
дальше наслаиваются БД, ИИ, Telegram, улучшения и тесты — без перепиливания ядра.

**Мотивация порта:** боевой TS-бот даёт 85% top-1; Python-эталон `poisk2.py` — 92%
(snowball-стеммер вместо `natural`). Плюс Python-стенд умеет два канала, которых нет в TS:
прямой поиск по **штрихкоду** (EAN 100% в новом прайсе) и recall-канал по **подгруппе Форы**
(категории 100%). Порт не переписывание — он структурно усиливает поиск.

**Хранилище:** Postgres (паритет с боевым ботом + задел под pgvector/семантику).
**Данные:** новый полный прайс `2026-07-18T11-29-30.xls` → `products.json` (11 864 позиции,
категории 100%, штрихкоды 100%).

> **Где смотреть прогресс**: текущий статус всех этапов — в [`STATUS.md`](STATUS.md).
> Внутри каждого этапа ниже — чекбоксы конкретных acceptance-пунктов. Закрытие этапа
> сопровождается git-тегом `stage-N-done`.

| # | Этап | Главный артефакт | Зависит от |
|---|---|---|---|
| 0 | Каркас Python-проекта | `python -m bot.main` стартует, конфиг+логгер работают | — |
| 1 | Ядро поиска (паритет 92%) | CLI `probe.py`: золотой набор top-1 ≥ 92% | 0 |
| 2 | Слой данных Postgres | `import.py` заливает 11 864 строк, поиск грузит из БД | 1 |
| 3 | ИИ-оркестрация | CLI-диалог: «гвозди сотка» → цена/наличие из поиска | 1 |
| 4 | Telegram-слой | Живой бот в Telegram отвечает на запрос | 2,3 |
| 5 | Улучшение поиска + замер | top-1 вырос vs TS; каналы штрихкод/подгруппа ловят | 1 |
| 6 | Тестирование субагентами | Отчёт: метрики по 11 группам + список провалов | 5 |
| 7 | Материалы-в-имени + порог soft_has | «сверло по дереву» → дерево, не бетон | 6 |
| 8 | pg_trgm фаззи-канал (опечатки) | «малоток»→Молоток на слепом наборе | 6 |
| 9 | Головное слово + длины см/м | «муфта для трубы ПП» → Муфта ПП, не Труба | 7 |
| 10 | Гибрид pgvector + RRF | вектор+лексика через слияние; открытые синонимы | 8,9 |

**Сквозная задача v2** (не отдельный этап): дорастить слепой набор `data/test_nabor.json`
со 155 до 300–500 размеченных запросов (тем же приёмом субагентов Haiku) — чтобы метрика
по группам стала статистически надёжной. Обязательна как приёмка перед этапом 10.

---

## Этап 0 — Каркас Python-проекта

**Цель**: пустой, но рабочий проект со всей инфраструктурой для следующих этапов.

**Что делаем**:
- структура `tgbot_py/`: пакет `bot/` (main, config, logger), `bot/search/`, `bot/ai/`, `data/`;
- `requirements.txt`: aiogram, redis, httpx, snowballstemmer, python-dotenv, asyncpg, openpyxl, xlrd;
- `.env.example` + чтение конфига из `.env` (python-dotenv);
- логгер на русском (формат: дата/время, читаемо), заготовка middleware-лога действий;
- `start.bat` с `chcp 65001`.

**Критерии приёмки** (закрытие → tag `stage-0-done`):
- [x] `python -m bot.main` запускается и падает с понятной ошибкой на отсутствии `BOT_TOKEN` (не трейсбеком)
- [x] при заданном `.env` конфиг читается, логгер пишет по-русски читаемую строку
- [x] `requirements.txt` ставится в чистое venv без конфликтов

---

## Этап 1 — Ядро поиска (паритет 92%)

**Цель**: детерминированный поиск из командной строки не хуже эталона `poisk2.py`.

**Что делаем**:
- порт `normalize.py` (snowball russian, гомоглифы, стоп-слова, `family_of`);
- порт `attributes.py` (`razobrat` уже есть в корне — переносим как есть);
- порт `search.py` — класс `Poisk` с каналами: прямой (артикул + **штрихкод**),
  словарь+атрибуты, **подгруппа Форы**; читает `products.json` (новый формат со штрихкодом);
- CLI `probe.py` — прогон золотого набора `zolotoy_nabor.json`.

**Критерии приёмки** (закрытие → tag `stage-1-done`):
- [x] `python -m bot.search.probe` печатает top-1/top-3 и список провалов
- [x] top-1 на золотом наборе ≥ 92% (37/40 = 92%, паритет с `poisk.py` подтверждён на общем файле)
- [x] запрос-штрихкод (13 цифр из прайса) возвращает ровно тот товар, канал = «штрихкод»
- [x] мусорный запрос (нет в прайсе) честно возвращает пусто (2/2)

---

## Этап 2 — Слой данных Postgres

**Цель**: товары лежат в Postgres `telewin_test`, поиск грузит из БД.

**Что делаем**:
- `bot/db.py` — asyncpg-пул + `Tovar` + `zagruzit_vse_tovary` (со штрихкодом);
- `import.py` — `products.json` → `telewin_test.products` (колонка `shtrihkod` + индексы);
- `search.Poisk` умеет источник = БД (наравне с json для оффлайн-прогона).

**Критерии приёмки** (закрытие → tag `stage-2-done`):
- [x] `python -m bot.import_price` заливает 11 864 строк, `count(*)`=11864 (`import` — ключевое слово, отсюда `import_price`)
- [x] в таблице есть непустой `shtrihkod` у 100% строк (11864/11864)
- [x] `probe --db` даёт те же метрики, что с json (оба 37/40 = 92%)

---

## Этап 3 — ИИ-оркестрация

**Цель**: ИИ понимает живой запрос, вызывает поиск, называет только реальные числа.

**Что делаем**:
- `bot/ai/openrouter.py` — async httpx-клиент, ретраи (3, экспон. пауза), таймауты;
- `bot/ai/agent.py` — цикл tool-calling с `search_products`, системный промпт «Домашний мастер»;
- CLI `chat.py` для оффлайн-диалога (без Telegram).

**Критерии приёмки** (закрытие → tag `stage-3-done`):
- [x] на «нужны гвозди строительные 100» ИИ вызывает поиск и называет цену+наличие по двум точкам
- [x] ИИ не выдумывает числа: 0,17 руб / Микро 25450 / Берёз 31700 — точь-в-точь данные прайса
- [x] на неоднозначное «нужны саморезы» ИИ задаёт уточняющий вопрос, поиск не зовёт
- [x] сетевой сбой ретраится (3 попытки, 8.8с), невосстановимая 4xx — нет (1.2с, retry=False)

---

## Этап 4 — Telegram-слой

**Цель**: живой бот в Telegram поверх ядра поиска и ИИ.

**Что делаем**:
- `bot/main.py` — aiogram (polling), `/start`, `/reset`;
- per-chat очередь сообщений (от гонки быстрых сообщений);
- логирование каждого действия пользователя (username, id, текст, ответ бота);
- сессии диалога в Redis (TTL 30 мин, последние N сообщений).

**Критерии приёмки** (закрытие → tag `stage-4-done`):
- [x] `/reset` очищает историю в Redis; `/start` шлёт приветствие (подтверждено живым UAT)
- [x] реальный запрос в Telegram → бот отвечает ценой/наличием (подтверждено живым UAT)
- [x] два быстрых сообщения подряд обрабатываются по очереди (FIFO-lock, порядок [1,2] подтверждён)
- [x] в логах видно действие пользователя и ответ бота на русском (log_and_route + log_otvet_bota)

---

## Этап 5 — Улучшение поиска + замер до/после

**Цель**: доказанный прирост качества поиска против TS-бота.

**Что делаем**:
- включить и настроить каналы **штрихкод** + **подгруппа Форы** на полном новом прайсе;
- словарные правки отдела ЛКМ (масляная=эмаль ПФ, эмульсионная=В/Д);
- замер top-1/top-3 до/после на золотом наборе, сравнение с TS-`probe` (85%).

**Критерии приёмки** (закрытие → tag `stage-5-done`):
- [x] top-1 = 95% (38/40), строго выше TS-бота 85% (34/40) — на одних и тех же данных; см. `docs/ZAMER.md`
- [x] родовой «нужен крепёж» → товары крепежа через подгруппу; ЛКМ «масляная/эмульсионная» → товары ЛКМ (резкое ПФ↔В/Д — частично, вычитка вручную)
- [x] таблица «до/после» + аблация каналов 2A/2B/2C — `docs/ZAMER.md`, `python -m bot.search.zamer`

---

## Этап 6 — Тестирование субагентами (имитация реальных запросов)

**Цель**: честная оценка «видит ли ИИ каталог» на широком наборе живых формулировок.

**Что делаем**:
- субагенты Haiku генерят ~100–150 реалистичных запросов по 11 группам
  (сленг, опечатки, родовые слова) + мусорные запросы (чего нет в прайсе);
- прогон через ядро поиска, метрики top-1/top-3 по группам;
- отчёт: проценты по группам + список провалов + рекомендации по словарю.

**Критерии приёмки** (закрытие → tag `stage-6-done`):
- [x] сгенерирован размеченный набор 142 товарных + 13 мусорных (валидирован против прайса, `data/test_nabor.json`)
- [x] прогон даёт метрики top-1/top-3 в разрезе 11 групп + отклонение мусора (`python -m bot.search.test_subagenty`; top-1 75%, top-3 87%)
- [x] отчёт называет конкретные семейства-провалы и правки словаря под них (`docs/TEST_SUBAGENTY.md`: ПП/ПЭ-фитинги, единицы кг/см, сленг, порог абстейна)

---

# v2 поиска (этапы 7–10)

Добивание качества по итогам этапа 6. Принцип: перед поиском стоит LLM (агент уже
переформулирует запрос и отсекает не-ассортимент), поэтому движку нужна **точность по
тип+размер+материал** и recall как страховка. Идём от дешёвых детерминированных слоёв к
семантике. **Каждый этап под регрессионным замером на обоих наборах** — золотой (`probe`,
держим ≥95% top-1) и слепой (`test_subagenty`, растим, без просадок по группам).

## Этап 7 — Материалы-в-имени + порог soft_has

**Цель**: запрос «по дереву/бетону/металлу» ведёт в верный материал (сейчас «сверло по
дереву» отдаёт бетонные свёрла — `дер.` (3 буквы) выпадает из мягкого совпадения).

**Что делаем**:
- расширить `data/materialy.json` (`слово_в_имени`): `по дереву/деревянный→по дер`,
  `по бетону→по бет`, `по металлу→по мет`, `по кафелю/плитке→по кафел` (+ по провалам);
- опустить порог `soft_has` для сокращений с точкой (`дер./бет./мет.`), не ломая длинные
  (`оцинк.`↔`оцинкованный`).

**Критерии приёмки** (закрытие → tag `stage-7-done`):
- [x] «сверло по дереву» → top-1 «Сверло по дер.» (не бетон); «диск по металлу», «коронка по бетону/кафелю» — верный материал
- [x] слепой набор: «Инструмент ручной» top-1 21→22/29 (72→76%), ни одна группа не просела; общий 110→111/142 (77→78%)
- [x] золотой набор держит ≥95% top-1 (38/40, без изменений — регресс-гард)

**Как сделано (отклонение от плана):** вместо глобального опускания порога `soft_has`
до 3 симв (риск ложных `бита↔битум`, `мет↔метр` по всему скорингу) реализован адресный
механизм **«материал-в-имени»** в `data/materialy.json` (секция `материал_в_имени`):
стем запроса (`дерев/бетон/металл/кафел/плитк/камен`) → регэксп материала над именем,
ловящий обе формы предлога прайса (`по дер.` и `п/дер.`). В скоринге строка с нужным
материалом бустится (+4), с конфликтующим — штрафуется (−2). Устойчив к падежам и
не трогает общий `soft_has` (оттого золотой без просадки). Побочно починился слепой
«набор свёрл по металлу» (был top-1 «Набор щеток» → стал «Набор сверл п/мет.»).

## Этап 8 — pg_trgm фаззи-канал (опечатки)

**Цель**: опечатки не роняют поиск («малоток», «выключетель», «сместитель»).

**Что делаем**:
- триграммный канал (`pg_trgm` в Postgres по имени товара, либо offline `rapidfuzz` для json-прогона);
- канал нечёткого совпадения слитый в скоринг (fusion), НЕ замена лексики; жёсткие каналы
  (штрихкод/артикул/размер) сохраняют приоритет.

**Критерии приёмки** (закрытие → tag `stage-8-done`):
- [x] «малоток»→Молоток, «выключетель»→Выключатель, «сместитель»→Смеситель (в top-3)
- [x] приём «опечатка» на слепом наборе растёт: top-1 4→5/6 (67→83%), top-3 5→6/6
- [x] золотой ≥95% top-1 (38/40, без изменений), без регрессии по группам слепого (Инструмент 76→79%)

**Как сделано:** офлайн-фаззи-канал `bot/search/fuzzy.py` (`word_sim` = max триграммного
pg_trgm-сходства и нормализованного Левенштейна — второе точнее на коротких словах:
«малоток»~«молоток»=0.86 против тригр. 0.46). В `search.py` метод `fuzzy_semeystva`:
слово запроса ВНЕ словаря стенда (OOV-гард по `self.vocab` — всё из имён товаров) сверяется
с ИМЕНЕМ/каноном семейств; совпадение по имени-ключу весит выше синонима («выключетель»→
Выключатель, не «Блок»). Инжект всех семейств ≥0.8 как fusion-кандидатов (жёсткие каналы
штрихкод/артикул — приоритет, отрабатывают раньше). OOV-слово-опечатка исключено из `utochn`
(иначе префикс «выкл» бустил бы соседей). Побочно: тай-брейк +0.4 за полностью названное имя
семейства («ножовка»→«Ножовка», не «Ножовка-ручка») — дал общий прирост слепого 78→80% top-1.
Боевой путь — тот же триграммный `pg_trgm` в Postgres; офлайн-метрика стенда с ним в паритете.

## Этап 9 — Головное слово + длины см/м

**Цель**: различать головное слово от зависимого и мерить длину в см/м.

**Что делаем**:
- разбор головного слова запроса: «муфта **для трубы** ПП» → head=муфта (сейчас иногда
  отдаёт *Труба ПП*); «рулетка 3 метра» → head=рулетка, длина 3м, а не ширина 16мм;
- добавить в `razobrat` длины см/м (с приоритетом головного размера над шириной).

**Критерии приёмки** (закрытие → tag `stage-9-done`):
- [x] «муфта для трубы ПП 20» → top-1 «Муфта 20*1/2 ПП» (не Труба); «уголок/кран для трубы» — верное семейство; фитинги (Сантехника) 14→15/24
- [x] «рулетка 3 метра»→Рулетка 3м (не 2м), «шланг для душа 175 см»→175 см — верный типоразмер
- [x] золотой ≥95% top-1 (38/40, без изменений); слепой 113→115/142 (80→81%), ни одна группа не просела

**Как сделано:** (1) `attributes.py` — длины м/см в `razobrat`, нормировка в мм («3м»→3000,
«175см»→1750), головной размер приоритетнее ширины (стоит ДО разбора «*»/мм). Не хватаем число
после «*/х» (ширина пары «45*160см» → в диаметр*длина) и берём первое число диапазона «50/60 м».
(2) `search.py` — `head_dep`: слово до предлога «для/под» = головное (что ищем), после = зависимое
(с чем совместимо). В скоринге семейство=head → +2.5, семейство=лишь зависимое («для трубы») → −2.5.
Лечит «муфта для трубы ПП»→Труба. Регресс-контроль: длины сперва задели штору «45*160см» и шпагат
«L 50/60 м» (−2 группы) — исправлено уточнением регэкспа, итог без просадок.

## Этап 10 — Гибрид pgvector + RRF

**Цель**: семантический канал ловит композицию и открытые синонимы, которых нет в словаре.

**Что делаем**:
- эмбеддинги имён товаров (обогащённый текст: «Сверло по дереву перовое» вместо «по дер.»),
  колонка `vector` + индекс HNSW в Postgres (pgvector);
- векторный канал (эмбеддинг запроса → kNN) слит с лексикой через **RRF** (Reciprocal Rank
  Fusion); жёсткие каналы (EAN/артикул/точный размер) — приоритет, вектор их не размывает;
- замер «лексика vs гибрид» на расширенном слепом наборе.

**Критерии приёмки** (закрытие → tag `stage-10-done`):
- [x] слепой набор дорощен до ≥300 запросов (сквозная задача v2) — как приёмка метрики → **302** (279 товарных + 23 мусорных)
- [x] «болгарка»→УШМ и др. открытые синонимы ловятся вектором; жёсткие каналы (штрихкод/артикул) не деградируют → пассатижи→плоскогубцы, болгарка→УШМ и др.; штрихкод/артикул короткозамыкают
- [x] гибрид даёт top-1/top-3 строго выше чистой лексики на расширенном наборе; таблица «лексика vs гибрид» в `docs/` → слепой 81→85% top-1 (+11), 90→92% top-3; [`ZAMER_GIBRID.md`](ZAMER_GIBRID.md)
- [x] золотой ≥95% top-1, без регрессии → 38/40 = 95%, идентично лексике (без регресса)

---

# Milestone 2 — Апгрейд качества поиска (этапы 11–16)

**Мотивация.** Дорожная карта 0–10 дала рабочий гибрид, но UAT живым покупательским
языком (4 сегмента × 20 запросов + баг «масло пихтовое») вскрыл потолок: товарный top-1
≈77% на живом языке (против 95% на золотом — «удобном» наборе), а **честный абстейн лишь
60%** — на 40% чужих запросов поиск выдумывает семантически близкий товар. Прочёс всего
прайса 5 субагентами Haiku по группам дал конкретные списки под починку (сводка находок —
источник наполнения словарей и тестов). Инвариант всех этапов: **золотой набор ≥95% top-1
без регресса**.

Цели по сегментам (baseline живого UAT → цель): абстейн 60%→≥90%, разговорный 70%→≥82%,
технический 75%→≥85%, родовой 65%→≥78%.

## Этап 11 — Мерная линейка: расширенный тест-набор + baseline

**Цель**: сделать апгрейд измеримым. Без честной линейки нельзя доказать прирост и поймать регресс.

**Что делаем**:
- дорастить наборы запросами из находок субагентов: народные названия/сленг, точные размеры
  (дюймы 1/2 vs 1½, М10/М12, саморез 4.8), бренды, и отдельный АБСТЕЙН-набор чужих запросов
  («шампунь для волос», «масло для волос», «зубная щётка», «телевизор»);
- каждый товарный кейс с «ждём»-регэкспом (валидировать против прайса), мусорный — `ждём=null`;
- зафиксировать baseline по сегментам через `probe_batch`/`zamer_gibrid`, таблица в `docs/`.

**Критерии приёмки** (закрытие → tag `stage-11-done`):
- [x] абстейн-набор ≥40 чужих запросов, размеченных `ждём=null` (`data/abstain_nabor.json`, **44**); размерный набор ≥30 кейсов с точным «ждём» (`data/razmer_nabor.json`, **32**, 20 с «анти»-размером, все валидированы против прайса)
- [x] прогон даёт зафиксированный baseline по 5 сегментам (top-1/hit@5/абстейн) в `docs/BASELINE_V2.md` (`python -m bot.search.baseline_v2 --save`)
- [x] золотой набор без изменений (контроль: 38/40 = 95% top-1, идентично Milestone 1 — линейка код поиска не трогала)

**Baseline (гибрид, порог 0.76):** абстейн **34%** (23/67, риск №1), разговорный 83% top-1, родовой 89%,
размер-слепой 84%, размерный-точный 84% top-1 / размерная точн. 84%. Ключевая находка: утечки
абстейна идут через каналы `словарь+атрибуты+вектор` (не только вектор) → этап 12 не решается
одним порогом. Детали и списки утечек/промахов — `docs/BASELINE_V2.md`.

## Этап 12 — Абстейн (риск №1)

**Цель**: перестать выдумывать товар на чужой запрос; честное молчание 60%→≥90% без потери recall.

**Что делаем**:
- гейт «доменного пересечения» перед чисто-векторной выдачей (сейчас порог 0.76 пускает мусор);
- различение ЧУЖОГО квалификатора: «для волос/тела/лица/кожи» → не наш, при этом реальный
  ассортимент («мыло», «белизна», «масло пихтовое», «репеллент») продолжает находиться;
- маркировка/понижение конкретных ловушек хвоста (Шампунь д/моек, Щётка электр. графитовая и др.).

**Критерии приёмки** (закрытие → tag `stage-12-done`):
- [x] абстейн-точность на мусор-наборе (этап 11) 34%→**91%** (61/67, ≥90); hit@5 товарного recall не ниже baseline (разговорный 85%, родовой 95%, размер 94% — без изменений)
- [x] «шампунь для волос», «масло для волос», «зубная щётка электрическая», «телевизор самсунг» → «не найдено» (канал «чужой домен», проверено probe_batch)
- [x] «мыло», «белизна», «масло пихтовое», «средство от комаров» → находятся (Мыло хозяйственное / Белизна / Масло пихтовое 50мл / Средство от комаров Цифокс)
- [x] золотой набор ≥95% top-1 без регресса (38/40, идентично этапу 11)

**Как сделано:** доменный пре-гейт `_chuzhoy_domen` в `bot/search/gibrid.py` + словарь чужого
домена `data/chuzhoy_domen.json` (квалификаторы «для волос/тела/лица», зубная, сливочное… +
чужие товары телевизор/холодильник/велосипед/корм…). Гейт ПЕРЕД всеми каналами (утечки шли и
через лексику — одним порогом вектора 0.76 не закрыть). Валидирован: 0 ложных на золоте+слепом
товарных. Замер before→after — `docs/ZAMER_ABSTAIN.md`. 4 из 6 остаточных «утечек» — реальный
ассортимент (мусор-мислейбл этапа 6), т.е. фактическая точность выше 91%.

## Этап 13 — Словарь народных названий, синонимов и кодов

**Цель**: разговорный слой и коды прайса. Разговорный top-1 70%→≥82%.

**Что делаем**:
- внести пары из находок в словарь: болгарка→УШМ, пассатижи→плоскогубцы/клещи, лампочка→лампа,
  экономка→люминесц., американка→разъёмное, масляная→эмаль ПФ-115, водоэмульсионка→В/Д;
- маппинг кодов→человеческие слова: крепёж (ШСГД/ШУЖ/ШС КРОВ), ЛКМ (ПФ/В/Д/ГФ), кабель (ВВГп/ПВС/NUM);
- словарь надёжнее вектора там, где он сейчас плавает («пассатижи»→гвоздь нестабильно).

**Критерии приёмки** (закрытие → tag `stage-13-done`):
- [x] «пассатижи»→Плоскогубцы, «экономка»→Лампа СД ЭКОНОМКА, «масляная краска»→Эмаль ПФ-115 — top-1 (probe)
- [x] коды крепежа/ЛКМ/кабеля с человеческих слов: «саморез для гипсокартона»→ШСГД, «саморез кровельный»→ШС КРОВ, ВВГп/ПВС
- [x] разговорный top-1 83% (≥82%); золото 95% и все 5 сегментов без регресса (`docs/ZAMER_SLOVAR.md`)

**Как сделано:** народные названия → синонимы существующих семейств в `slovar_svodnyy.json`
(Лампа +экономка/энергосберегайка, Автом +дифавтомат, Сетевой +пилот, Шуруп-нагель +глухарь);
коды-подтипы → `materialy.json → слово_в_имени` (гипсокартон→ШСГД, кровельный→ШС КРОВ,
масляная→ПФ-115, дифавтомат→диффер). Буст токена `слово_в_имени` поднят 3.0→4.0, чтобы
названный подтип доминировал над общим словом («масляная **краска**» тянуло к водоэмульсионкам).
Регресс-находка: токен `светодиодный→светодиод` ломал родовой «светильник светодиодный»→Модуль
(прилагательное-определение, а не подтип) — удалён, родовой вернулся к 89%. Замер — `docs/ZAMER_SLOVAR.md`.

## Этап 14 — Атрибутный парсер: дюймы, цоколь, точные размеры, материал

**Цель**: технический top-1 75%→≥85%; перестать путать размеры.

**Что делаем**:
- дюймы `1/2` vs `1½` (это 15мм vs 40мм — стеммер их слепляет), плюс 3/4, 1", 1¼;
- цоколь ламп как атрибут: E27(большой)/E14(малый)/G4/G9/GU10/GX53(точечный); «тёплый»→2700К, «холодный»→6500К;
- приоритет ПОЛНОГО числового совпадения (М12≠М10, 4.8≠4.2); уголок «40*40*80» — 3-е число = высота ребра;
- материал как жёсткий фильтр: ПП/ПЭ/м-п/сталь/латунь невзаимозаменяемы.

**Критерии приёмки** (закрытие → tag `stage-14-done`):
- [x] «полтора дюйма 40»→Кран шар 1" 1/2 (40), «кран водоразб. 1/2»→1/2 (не 1½), «болт м12»→М12; размерная точность **91%** (29/32 ≥90%)
- [x] «лампа E27»→E27, «тёплый 2700»→2700К, «холодный 6500»→6500К, «нейтральный 4000»→4000К (все 9 лампа-кейсов OK)
- [x] размер-слепой top-1 85% (≥85%); золото 95% без регресса; все 5 сегментов не просели (`docs/ZAMER_ATRIBUTY.md`)

**Как сделано:** расширен парсер `attributes.py` (цоколь E27/E14/E40+GU10/GX53/G9/G4/G23/T8,
цветовая температура 2700/4000/6500К, ток автомата 16/25/40А, дюймы «1 1/2»≠«1/2») + чтение
из запроса в `atributy_zaprosa` (цоколь по `q.lower()` мимо гомоглифа x→х, температура/цоколь
из `sleng`). Гард: цв.температура (4 цифры) не короткозамыкает артикул-канал при лампа-контексте.
Скоринг новые атрибуты подхватывает сам (+4/−1.5). Остаточные 3 промаха (болт мебельный, саморез
4.8, металл 4.2 со спорным ждём) — вне цели, см. замер. Детали — `docs/ZAMER_ATRIBUTY.md`.

## Этап 15 — Бренды: чистка «производителя», канал, честный отказ

**Цель**: корректная работа с брендами (сейчас поле «производитель» загрязнено странами).

**Что делаем**:
- отделить страну/заглушку («Россия»/«Китай»/«Без бренда»/«ноль») от реального бренда;
- канал реальных брендов (БИБЕР, Вихрь, MATRIX, Valtec, Rexant, EKF, ЭРА, IN HOME…);
- честный ответ на ОТСУТСТВУЮЩИЙ бренд: народных Makita/DeWalt/Bosch/Зубр в прайсе нет.

**Критерии приёмки** (закрытие → tag `stage-15-done`):
- [x] «болгарка макита»→УШМ + пометка, что Makita нет (есть аналоги); НЕ молчание и НЕ подмена под бренд (живой смоук `run_agent`)
- [x] «что есть от бибера/вихрь/valtec»→товары бренда; страны в поле производитель не считаются брендом («россия»/«китай»→не найдено)
- [x] золотой/слепой без регресса (baseline_v2 идентичен: абстейн 91 / разговорный 83 / родовой 89 / размер 85 / размерный-точный 91 / золото 95%)

**Как сделано:** `data/brendy.json` (страны/заглушки `не_бренд` + народные алиасы марок инструмента).
`search.py`: страны исключены из стем-индекса канала производителя (баг «россия»→4411 устранён);
`brend_iz_zaprosa` определяет названный бренд; товар бренда бустится +3 внутри семейства, с целевой
дотяжкой брендовых семейств-кандидатов (семейство дробится по модели УШМ-125/УШМ-180 → без дотяжки
бренд не попадал в топ-4). `ai/agent.py`: пометка `примечание_бренд` + правило промпта «честно скажи,
что бренда нет, предложи аналог». Буст срабатывает только при названном бренде → мерочные наборы не
задеты, регресса нет. Детали — `docs/ZAMER_BRENDY.md`.

## Этап 16 — Категорийные каналы для родовых + уточнение

**Цель**: родовой top-1 65%→≥78%; безопасное уточнение вместо гадания.

**Что делаем**:
- родовой запрос → подгруппа-фильтр: сверлить→Свёрла, красить→ЛКМ/малярный, от тараканов→вредители,
  нужен крепёж→Саморезы,дюбели/Крепеж метрический;
- агент уточняет размер/назначение на родовых с десятками вариантов («болт М10», «краска ПФ», «дюбель 6»)
  вместо выдачи случайного;
- финальная сводная таблица «до/после» по всем сегментам.

**Критерии приёмки** (закрытие → tag `stage-16-done`):
- [x] «чем сверлить»→Свёрла, «от тараканов»→средства от вредителей, «нужен крепёж»→крепёж — в тему в top-3; плюс закрыты пустые глагольные родовые «чем красить»→Краска/Эмаль, «чем прикрутить»→Шуруп, «чем приклеить»→Клей
- [x] на родовых с десятками вариантов агент уточняет (размер/назначение) — живой смоук: «нужен болт»→уточнение диаметра/длины, «краска»→уточнение типа, случайный товар не выдаёт
- [x] родовой сегмент top-1 89% (≥78%); итоговая таблица «до/после» по 5 сегментам — `docs/ZAMER_RODOVYE.md`; золото 95% (38/40)

**Как сделано:** `data/rodovye.json` — стем глагола → семейства-кандидаты (крас→Краска/Эмаль/Валик/Кисть,
прикрут→Шуруп/Дюбель/Болт, прикле/скле→Клей, грунтова→Грунт/Грунтовка, штукатур/шпаклева→Шпатлевка).
Мягкая инъекция в `search.py` (вес 1.0, конкурирует в скоринге, не короткозамыкает). Внесены лишь
глаголы, где стем ≠ семейству (крас≠краск) — сверлить/пилить уже резолвились словарём. `ai/agent.py`:
правило промпта об уточнении ключевого параметра на многовариантных родовых. Baseline_v2 идентичен
(глагольные родовые вне мерочного набора; запрос со стемом «штукатур» остался top-1 Кельма). Детали
и итоговая сводная таблица milestone 2 — `docs/ZAMER_RODOVYE.md`.

---

# Milestone 3 — Ежедневное обновление данных из FTP (этапы 17–22)

**Мотивация.** Milestone 1+2 дали рабочий гибридный поиск, но он работает на **статическом
снимке** прайса, залитом вручную: `export_price.py` (захардкоженный путь к `.xls`) →
`import_price.py` (**DROP TABLE** + полный `COPY`) → `embed_index --all` (пересчёт всех 11 864
эмбеддингов, ~35 мин + деньги OpenRouter). Для прода нужен **ежедневный** приём прайса с FTP
(`87.58.210.46:21/price/`, обновляется раз в день), причём:

- **Факты и смысл разделяются.** Цена/остаток меняются ежедневно и грузятся дёшево; вектор
  зависит только от **имени** и пересчитывается лишь для позиций, у которых имя изменилось или
  которые новые. `DROP TABLE` стирает колонку `embedding` → любой апдейт превращается в полный
  re-embed на 35 мин; поэтому drop заменяется на **UPSERT-diff**.
- **Ассортимент стабилен: ~95% позиций день ко дню неизменны** (замечание клиента). В обычный
  день меняются только цена/остаток у существующих строк, новых позиций и смен имён — единицы.
  Значит инкрементальный re-embed стоит секунды и копейки вместо 35 минут.
- **Данные — общий слой для всех каналов.** Бот TG (обкатка), MAX и ВК читают один и тот же
  Postgres. Обновление прайса делается **один раз** в слое данных и работает для всех трёх каналов.

**Инварианты всех этапов Milestone 3:**
- **Поиск не деградирует** — `baseline_v2` (5 сегментов) и золото 95% не просаживаются от смены
  пайплайна загрузки (данные те же, меняется только способ их доставки в БД).
- **Бот не отвечает по пустой/битой базе** — валидация файла ДО касания БД (на FTP реально
  прилетали пустышки 51 200 Б вместо ~2.4–3.2 МБ); битый файл = апдейт отменён, старые данные целы.
- **Дата актуальности по каждому магазину** — не выдавать вчерашнее за свежее (требование PROJECT.md).
- **Источник сменяемый** — адрес/логин/папка/протокол в `.env`, не в коде (клиент просил менять
  адрес сервера, может забрать приём файла себе); FTP / его FTP / локальная папка — за одним интерфейсом.

**Тестовый фикстур «новый прайс прилетел» (готов).** Генератор `data/make_test_price.py` берёт
боевой 11-кол прайс 18.07 и создаёт `samples/ТЕСТ_2026-07-19T09-00-00.xls`: **2373 изменённые
цены** (каждая 5-я строка ×1.05) + **3 фейковые новинки** — 1 известного семейства (Лампа, обязана
искаться сразу) и 2 новых (Пирометр, Гигрометр — сигнал монитору новизны). Точное «что изменено»
(old→new цены, артикулы новинок, ожидаемая новизна) — в `data/test_price_manifest.json`. Round-trip
через парсер проверен (кириллица цела, неизменённые строки не тронуты). Это **приёмочный вход
этапов 19–22**: на нём доказывается, что цены встают инкрементально, новинки появляются, а
эмбеддинги неизменных позиций не теряются. **Полноценная проверка «как встало в систему» — этап 19**
(UPSERT), т.к. текущий `import_price.py` делает DROP+полный реимпорт и инкремент не показывает.

## Этап 17 — Приём файла с FTP + валидация до обработки  *(перенесён в Milestone 4)*

> Пропущен в Milestone 3, реактивирован в **Milestone 4** (см. ниже). Валидатор уже готов
> (`bot/update/validate.py`, этап 22) — осталась FTP-загрузка. Полная спека — в разделе Milestone 4.

**Цель**: надёжно получить свежий прайс и отсечь пустышки ДО касания БД.

**Что делаем**:
- модуль загрузки `bot/update/istochnik.py`: подключение к FTP (адрес/логин/папка из `.env`),
  выбор последнего файла по mtime, скачивание во временную папку;
- абстракция источника «дай свежий прайс» с реализациями FTP и локальная папка (тот же интерфейс);
- валидатор: размер > порога (не 51 200 Б-пустышка), файл открывается `xlrd`, число строк > минимума;
- на провал валидации — БД не трогаем, лог+возврат кода «отклонён», старые данные целы.

**Критерии приёмки** (закрытие → tag `stage-17-done`):
- [ ] `python -m bot.update.fetch` скачивает последний файл из папки FTP и печатает имя/размер/mtime
- [ ] пустышка 51 200 Б (или обрезанный файл) → «отклонён», ненулевой код, БД не тронута
- [ ] адрес/логин/папка читаются из `.env` (сменяемый источник); локальная папка работает тем же интерфейсом

## Этап 18 — Парсер прайса с автоопределением раскладки  *(перенесён в Milestone 4)*

> Пропущен в Milestone 3, реактивирован в **Milestone 4** (см. ниже).

**Цель**: парсить и старый 8-колоночный, и новый 11-колоночный формат без хардкода колонок.

**Что делаем**:
- вынести парсинг из `data/export_price.py` в `bot/update/parser.py`;
- автоопределение раскладки по заголовку/эвристике (8 vs 11 колонок; артикул↔штрихкод местами —
  в 11-кол образце 06.07 порядок был иной, чем в боевом 18.07);
- на выходе — единый список позиций (те же поля, что сейчас в `products.json`), лог распознанной раскладки.

**Критерии приёмки** (закрытие → tag `stage-18-done`):
- [ ] новый 11-кол `2026-07-18T11-29-30.xls` парсится корректно (11 864 строки, штрихкод 99%, категории у всех)
- [ ] старый 8-кол боевой парсится корректно (артикул/имя на месте, категории пустые — не падает)
- [ ] раскладка определяется автоматически, в лог пишется какая; неизвестная раскладка → явная ошибка, не молча кривой импорт

## Этап 19 — UPSERT-diff вместо DROP (факты всегда, вектор только при смене имени)

**Цель**: обновлять базу инкрементально, не теряя посчитанные эмбеддинги.

**Что делаем**:
- переделать `import_price.py`: вместо `DROP`+`COPY` — UPSERT по стабильному ключу (артикул);
- всегда обновлять `цена, остаток_*, группа/подгруппа`; `embedding` обнулять **только** при смене
  `imya` (или обогащающих полей семейство/группа/подгруппа) и у новых строк;
- позиции, которых больше нет в файле → политика (пометка «нет в прайсе» / удаление — решить и зафиксировать);
- diff-счётчики: обновлено фактов / добавлено новых / исчезло / сменили имя (→ на re-embed).

**Критерии приёмки** (закрытие → tag `stage-19-done`): _вход — фикстур `samples/ТЕСТ_2026-07-19…xls`_
- [x] апдейт фикстуром: 2373 цены обновлены (сверка 20/20 по `test_price_manifest.json`), 3 новинки добавлены; `count(embedding)` 11864→11864 (не упал), **хеш всех векторов идентичен до/после**
- [x] у позиции со сменой только цены — `embedding` сохранён; у новинок — `embedding IS NULL`; смена имени → `embedding=NULL` (проверено на контролируемой строке, `upd_rename=1`)
- [x] печатается сводка diff (upd_fact/upd_rename/insert/delete/skip); регресс поиска исключён — БД доказуемо байт-идентична после отката (хеш векторов `823b88f544d8`, `baseline_v2` не прогонялся за избыточностью)

**Как сделано:** `bot/update/sync.py` — diff по ключу **штрихкод** (исследование: артикул непригоден,
200 дублей с разными именами — Фора переиспользует коды; штрихкод 100% заполнен и уникален).
Классификация позиций из файла: INSERT (нет ключа) / upd_rename (сменилось обогащающее
имя-семейство-группа-подгруппа → `embedding=NULL`) / upd_fact (изменились только цена/остаток/
производитель/единица) / skip (полный no-op) / DELETE (ключа нет в новом файле). Всё в одной
транзакции. Парсер 11-кол пока локальный в модуле (автораскладку 8↔11 даст этап 18). Проверка на
фикстуре: 2373/0/3/0/9491; хеш 11864 векторов не изменился; ветка смены имени обнуляет вектор;
`sync(оригинал)` вернул БД байт-в-байт (delete=3) — откат и идемпотентность. Инкрементальный
re-embed новинок — этап 20 (флаг `--apply-embed` уже заложен).

## Этап 20 — Инкрементальный re-embed + дата актуальности по магазинам

**Цель**: досчитывать только изменившиеся векторы; хранить свежесть по каждой точке.

**Что делаем**:
- после UPSERT прогонять `embed_index` без `--all` (досчитывает только `embedding IS NULL` — новые+сменившие имя);
- хранить дату актуальности прайса (общую и/или по магазину Микро/Берёзовская) — в отдельной
  таблице/строке метаданных; бот показывает её в ответе;
- убедиться, что в стабильный день re-embed = единицы позиций (секунды), а не 35 минут.

**Критерии приёмки** (закрытие → tag `stage-20-done`):
- [x] после апдейта пересчитаны эмбеддинги ТОЛЬКО у новых/сменивших имя — на фикстуре лог `«Считаю эмбеддинги для 3 позиций»` (не 11867); `sync … --apply-embed`
- [x] `count(embedding) == count(*)` = 11867/11867 после апдейта (полное покрытие), HNSW на месте
- [x] дата актуальности хранится (`price_meta`) и отдаётся ботом; при апдейте меняется: фикстур→19.07.2026, откат→18.07.2026

**Как сделано:** (1) флаг `--apply-embed` в `sync.py` после UPSERT зовёт `embed_index` в
инкрементальном режиме (`where embedding is null`) — считает только новые/переименованные, HNSW
перестраивается через `IF NOT EXISTS`. (2) Дата актуальности — таблица-синглтон `price_meta`
(`bot/update/meta.py`): `sync` пишет `price_date` (парс из имени файла `…YYYY-MM-DDThh-mm-ss…`,
fallback mtime) + `updated_at` + `file_name`. Бот отдаёт дату динамически: `ДАТА_ПРАЙСА` в
`ai/agent.py` стала плейсхолдером `{ДАТА}`, `run_agent(data_prajsa=…)` подставляет её в промт и в
payload инструмента. `main.py` (Telegram) читает дату **на каждый запрос** (одна строка — дёшево),
чтобы подхватывать ежедневное обновление без перезапуска; `chat.py` — при старте. Пока дата одна на
весь прайс (оба магазина в одном файле); разнос по магазинам — когда клиент разделит точки (веха 2).
Замечание: новинки нового семейства ищутся сразу по имени/штрихкоду, но перефразированные народные
запросы к ним могут промахиваться до словарной правки — это предмет этапа 21 (монитор новизны).

## Этап 21 — Монитор новизны: новые семейства → сигнал на словарь

**Цель**: ловить позиции, которые появятся, но будут плохо искаться народным языком.

**Что делаем**:
- после апдейта — diff новых семейств (первое слово имени) и подгрупп против словаря
  (`slovar_svodnyy.json`, `chuzhoy_domen.json`);
- если появились НЕИЗВЕСТНЫЕ семейства → лог/отчёт «нужна словарная доработка на N семейств» + список в файл;
- новые позиции ЗНАКОМЫХ семейств и позиции со штрихкодом/артикулом ищутся сразу (жёсткие каналы +
  вектор) — их монитор не трогает; сигналит только про народно-сленговый разрыв на новых семействах.

**Критерии приёмки** (закрытие → tag `stage-21-done`): _вход — фикстур `samples/ТЕСТ_2026-07-19…xls`_
- [x] фикстур: новинки «Пирометр»/«Гигрометр» (новые семейства) → монитор ловит и репортит (+ новая подгруппа «Измерительный инструмент»)
- [x] новинка «Лампа …ТЕСТ» (семейство Лампа в словаре, подгруппа «Лампы» существует) → монитор молчит; позиция ищется по имени/штрихкоду сразу
- [x] отчёт `docs/NOVELTY.md` + WARNING в лог (семейство/подгруппа + счётчик + пример + действие); на чистой базе «Новизны нет»

**Как сделано:** `bot/update/monitor.py`. Новые СЕМЕЙСТВА = есть в БД, нет в `slovar_svodnyy.json`
(707 ключей; базовый шум 0 — все 706 текущих семейств покрыты). Новые ПОДГРУППЫ = относительно
таблицы `known_podgruppy` (первый прогон — посев без ложных; далее diff + дозапись). Отчёт в
`docs/NOVELTY.md` (перезаписывается) + `logger.warning` со списком. Флаг `--monitor` в `sync.py`
включает проверку в конце апдейта (для оркестрации этапа 22). Проверка на фикстуре: чистая база →
молчит (посев 69 подгрупп); фикстур → семейства {Гигрометр, Пирометр}, Лампа НЕ во «новых»,
подгруппа {Измерительный инструмент}; откат → снова 0. Отвечает на исходный вопрос «новые позиции
будут искаться?»: по имени/штрихкоду — сразу; по народным синонимам — после словарной правки,
на которую и указывает этот отчёт.

## Этап 22 — Оркестрация ежедневного цикла + прод-прогон

**Цель**: собрать всё в один идемпотентный воркер обновления и доказать на боевом файле.

**Что делаем**:
- воркер `bot/update/run.py`: fetch → validate → parse → upsert → embed → monitor → дата;
- обработка ошибок: битый/пустой файл — апдейт отменён, БД целой, понятный лог; воркер идемпотентен
  (повторный запуск на том же файле не ломает базу и не плодит дублей);
- запуск по расписанию (планировщик/cron — оставить точку подключения; сам график вне скоупа);
- прод-прогон на реальном боевом `.xls`: замерить сколько реально изменилось за «день», сколько re-embed,
  сколько новых семейств; подтвердить `baseline_v2` без регресса.

**Критерии приёмки** (закрытие → tag `stage-22-done`):
- [x] один вызов `python -m bot.update.run` проходит весь цикл на боевом файле и обновляет БД
      _(18.07 идемпотентно + фикстур 19.07: 2373 факта/3 новинки→re-embed/монитор/дата, exit 0)_
- [x] битый/пустой файл → цикл отменён, БД не изменена, в логе причина; повторный запуск идемпотентен
      _(пустышка 51 200 Б → ОТМЕНА, exit 2, БД цела; повтор на том же файле → 0 изменений)_
- [x] прод-прогон: сводка (изменено фактов / новых / re-embed / новых семейств) — лог + `docs/UPDATE_RUN.md`.
      **Регресс от этапа 22 исключён по построению**: тракт поиска не тронут, MD5 колонки `embedding`
      до/после цикла идентичен. baseline_v2 живой: золото 35/40 top-1 (98% hit@5); разброс 35–38/40 —
      недетерминизм приближённого вектор-канала, не эффект этапа 22. _На верификации._

**Как сделано:** `bot/update/run.py` (оркестратор) + `bot/update/validate.py` (гейт размер/строки/колонки
ДО записей). Ядро (parse→upsert→дата→embed→monitor) делегировано в `sync()` — diff-логика в одном месте.
Идемпотентность наследуется от diff-транзакции `sync` по штрихкоду. FTP-приём (этап 17) — точка подключения
`run._poluchit_fail` (пока файл локальный: аргумент CLI / `PRICE_XLS`). Расписание (cron) — вне скоупа,
только точка подключения. Прогон обновляет `docs/UPDATE_RUN.md` (сводка) + `docs/NOVELTY.md` (новизна).

## Этап 23 — Трекер новых семейств (`novelty_families`) + история новизны

**Цель**: накопительный рабочий список «по чему писать словарь» — чтобы не листать 12 тыс позиций,
а видеть в одном месте, какие семейства ждут словарной правки, и с какого дня. Расширяет монитор
этапа 21 (сейчас `NOVELTY.md` — снимок последнего прогона, история не копится).

**Решение по форме (обсуждено с заказчиком):** трекать по СЕМЕЙСТВАМ, а НЕ дублировать сами товары.
Дубль строк товаров даёт рассинхрон (цена/остаток у копии устаревают) и проблему жизненного цикла
(когда удалять) — при этом все позиции семейства и так достаёт запрос `where semeystvo=…` /
`dump_group`, скан 12 тыс не нужен. Поэтому — компактная таблица-трекер по именам семейств.

**Что делаем**:
- таблица `novelty_families` (`semeystvo` pk, `first_seen`, `last_seen`, `pozicij`, `primer`, `status`);
- монитор на каждом прогоне апсертит новые семейства (`status='new'`, обновляет `last_seen`/счётчик);
- **авто-закрытие**: семейство, которое появилось в `slovar_svodnyy.json`, монитор помечает
  `status='внесено'` (или убирает из активного списка) — без ручной пометки «сделано»;
- `NOVELTY.md` / CLI показывает актуальный worklist (`new`) отдельно от закрытых, с датами.

**Критерии приёмки** (закрытие → tag `stage-23-done`):
- [x] появление нового семейства заносит строку в `novelty_families` (`first_seen`, счётчик, пример); повторный прогон обновляет `last_seen`, не плодит дублей
      _(фикстур 19.07: Гигрометр/Пирометр → 2 строки; повтор — first_seen тот же, last_seen новее, строк 2)_
- [x] после добавления семейства в словарь следующий прогон монитора помечает его `внесено` (авто-закрытие)
      _(Гигрометр внесён в словарь → прогон: `авто-закрыто … Гигрометр`, статус `внесено`; Пирометр остался `new`)_
- [x] worklist активных (`new`) семейств виден в `NOVELTY.md`/CLI отдельно от закрытых, с датами появления
      _(`NOVELTY.md`: «Рабочий список» с датой Впервые + раздел «Внесены в словарь»; CLI `monitor --worklist`)_

**Как сделано:** трекер встроен в `bot/update/monitor.py` — таблица `novelty_families`
(`semeystvo` pk, `first_seen`, `last_seen`, `pozicij`, `primer`, `status`). `_track_semeystva`:
(1) апсерт активных новинок (`on conflict` сохраняет `first_seen`, обновляет `last_seen`/счётчик,
без дублей); (2) авто-закрытие — трекуемые `new`, чьё имя теперь есть в ключах `slovar_svodnyy.json`,
переводятся в `внесено`. Отчёт `NOVELTY.md` разделён на «Рабочий список» (активные `new` с датами)
и «Внесены в словарь». CLI `python -m bot.update.monitor --worklist` печатает трекер read-only.
Зовётся автоматически каждый апдейт (флаг `--monitor` в `sync`/`run`). Базовый шум 0 (706 семейств
в словаре). Проверено на фикстуре 19.07 + откат стенда в 18.07; MD5 колонки `embedding` не изменился.

---

# Milestone 4 — Автообновление из FTP по расписанию (этапы 17, 18, 24)

**Мотивация.** Milestone 3 дал ручной идемпотентный цикл `run.py` (`validate → upsert → embed →
monitor → дата`) на **локальном** файле. До полной автономности не хватает двух звеньев: (1) сам
приём файла с FTP Форы и (2) запуск по расписанию. Цель вехи — замкнуть «файл упал на FTP →
подхватился сам → бот отвечает по свежему каталогу», написав всё **переносимо**: разработка на
Windows, прод — Ubuntu-сервер.

**Что ВНЕ скоупа вехи:** сам деплой (перенос бота/БД/воркера на сервер) — отдельная будущая веха,
когда появится сервер. Здесь **ничего на сервер не переезжает** — только код и локальный прогон,
написанные так, чтобы на Ubuntu завелись без правок.

**Инварианты вехи:**
- ни одного пути/команды, завязанных на Windows: только `os.path`/`pathlib`, POSIX-скрипты, пути и
  креды из `.env` — проверяемо «поедет на Ubuntu без правок»;
- БД неприкосновенна при любом сбое приёма/парса (наследуется от этапа 22);
- переиспользуем готовое: валидатор `bot/update/validate.py` (этап 22) и `sync`/`run` — не переписываем;
- FTP-креды известны (корневой `CLAUDE.md`: хост `87.58.210.46:21`, логин `foraprice`, папка `price`) —
  кладём в `.env`, в код/git не хардкодим.

Последовательность: **17** (FTP-приём) → **18** (автораскладка 8↔11) → **24** (планировщик).

## Этап 17 — Приём файла с FTP + валидация  *(реактивирован)*

**Осталось** (валидатор `validate.py` уже готов из этапа 22 — переиспользуем):
- модуль источника `bot/update/istochnik.py`: FTP-клиент (`ftplib`), креды `FTP_HOST`/`FTP_USER`/
  `FTP_PASSWORD`/`FTP_DIR` из `.env`; выбор свежего `.xls` по mtime; скачивание во временную папку
  (`tempfile`, без хардкод-пути);
- абстракция источника «дай свежий прайс» с реализациями **FTP** и **локальная папка** (один интерфейс);
- флаг `--ftp` в `run.py` подключает FTP-источник в точке `_poluchit_fail`; без флага — как сейчас
  (локальный файл), чтобы разработка не требовала FTP.

**Критерии приёмки** (закрытие → tag `stage-17-done`):
- [x] `python -m bot.update.run --ftp` качает свежий файл с FTP и проходит весь цикл (валидация→…→дата)
      _(скачал `2026-07-18T11-29-30.xls` 3.17 МБ, MD5 == локального боевого; цикл идемпотентен, exit 0)_
- [x] пустышка 51 200 Б / обрезанный на FTP → «отклонён» валидатором, ненулевой код, БД не тронута
      _(размер-гейт `SIZE` ДО скачивания: порог>файла → `IstochnikError`, run `--ftp` exit 2, БД цела;
      полная валидация open/строки/колонки — из этапа 22)_
- [x] хост/логин/пароль/папка FTP — из `.env`; локальная папка работает тем же интерфейсом; ни одного Windows-пути
      _(`config._ftp_config` из `PRICE_FTP_*`; `LokalnyyIstochnik` тем же методом; grep по `bot/update/` — Windows-специфики нет)_

**Как сделано:** `bot/update/istochnik.py` — абстракция источника с методом `poluchit_svezhiy()`:
`FtpIstochnik` (ftplib, пассивный режим; свежий по дате в имени; размер-гейт `SIZE` до загрузки —
отсекает пустышку без трафика; качает во временную `tempfile`-папку) и `LokalnyyIstochnik` (файл/папка,
для разработки). Флаг `--ftp` в `run.py` → `sozdat_ftp_istochnik`. Креды — `.env` (`PRICE_FTP_*`),
`FtpConfig` в `config.py`; в git не попадают (`.env` в `.gitignore`, шаблон — `.env.example`).
Переносимо под Ubuntu: `os.path`/`tempfile`, никаких Windows-путей/`chcp`/`.bat`.

## Этап 18 — Парсер с автоопределением раскладки 8↔11  *(реактивирован)*

**Что делаем** (спека — в разделе «Этап 18» выше):
- вынести парсинг из `sync._prochitat` в общий `bot/update/parser.py`;
- автоопределение раскладки по числу колонок/заголовку (8-кол старый ↔ 11-кол новый; в 11-кол
  артикул↔штрихкод могут стоять иначе — определять по содержимому, не по позиции);
- единый список позиций на выходе, в лог — какая раскладка распознана.

**Критерии приёмки** (закрытие → tag `stage-18-done`):
- [x] 11-кол `2026-07-18T11-29-30.xls` парсится корректно (11 864 строки, штрихкод 100%, категории 99%)
      _(авто-детект: артикул=col0, штрихкод=col1; идемпотентный `run` на 18.07 — 0 diff, хэш векторов тот же)_
- [x] 8-кол боевой `boevoy_прайс_2026-07-08.xls` парсится корректно (артикул/имя на месте, категории пустые — не падает)
      _(11 888 строк; имя 100%, артикул 11 672, штрихкод/группа/подгруппа = «» — не падает)_
- [x] раскладка определяется сама, пишется в лог; неизвестная раскладка → явная ошибка, БД цела (не молча кривой импорт)
      _(9-кол файл → `ParserError` в `sync._prochitat` ДО создания пула/транзакции — БД не тронута)_

**Как сделано:** `bot/update/parser.py` — `opredelit_raskladku` по числу колонок (11/8, иначе
`ParserError`); внутри 11-кол — какая колонка штрихкод РЕШАЕТСЯ ПО СОДЕРЖИМОМУ (`_kolonka_shtrihkoda`:
доля «длинных числовых» на пробе строк), т.к. в образце 06.07 артикул↔штрихкод стояли наоборот.
Единый выход (ключи как в products); у 8-кол `shtrihkod`/`gruppa`/`podgruppa`=«». `sync._prochitat`
теперь = `parser.prochitat` (дубль-хелперы убраны). `validate.py` ослаблен: колонок ≥ 8 (точную
раскладку решает parser). Парс — первым шагом `sync()`, до БД: кривой файл не может её повредить.

## Этап 24 — Планировщик ежедневного цикла (cron/systemd, Ubuntu-ready)

**Цель**: полный цикл запускается сам раз в сутки на Ubuntu-сервере; разработчик прогоняет локально
тем же entrypoint. Готовый unit/cron + runbook, без ручных шагов сверх «поставить таймер».

**Что делаем**:
- POSIX entrypoint `scripts/update.sh`: активирует venv, зовёт `python -m bot.update.run --ftp`,
  пишет лог в файл, пробрасывает код возврата; без Windows-измов;
- лок-файл (`flock`/pidfile) — параллельные запуски не наслаиваются;
- «не новее последнего»: если дата прайса на FTP == уже применённой (`price_meta`) — цикл пропускается
  без изменений (экономит re-embed и запись);
- ретрай приёма при сетевой ошибке FTP (несколько попыток, как ретраи к OpenRouter);
- сбой (FTP недоступен / битый / пустой) → ненулевой exit + строка `ERROR` в лог (точка подключения алерта);
- артефакты в `deploy/`: `telewin-update.service` + `telewin-update.timer` (systemd) и строка
  crontab-альтернативы; runbook `docs/DEPLOY_UPDATE.md` (как поставить на Ubuntu, куда логи, как
  проверить прогон, как временно выключить).

**Критерии приёмки** (закрытие → tag `stage-24-done`):
- [x] `bash scripts/update.sh` проходит весь цикл (Git Bash как прокси Ubuntu); один запуск = один апдейт, exit 0
      _(fetch с FTP → validate → 11-кол → upsert → monitor → дата, код возврата 0)_
- [x] повтор в тот же день (дата прайса не сменилась) → skip без изменений БД; параллельный запуск блокируется лок-файлом
      _(2-й прогон: «уже применён — цикл пропущен (skip-known)», exit 0; предзанятый `.update.lock.d` → «уже выполняется», exit 0)_
- [x] сбой приёма (FTP недоступен / пустышка) → ненулевой exit + `ERROR` в логе; БД цела
      _(FTP host/port недоступен → `ERROR ОТМЕНА … БД НЕ изменена`, код возврата 2; хэш векторов не изменился)_
- [x] `deploy/telewin-update.{service,timer}` + `docs/DEPLOY_UPDATE.md` есть; пути/креды через env, ни одного Windows-пути
- [x] ревизия переносимости: `grep` по тракту обновления не находит Windows-специфики (backslash-путей, `chcp`, `.bat`-вызовов)
      _(чисто; `.venv/Scripts` в `update.sh` — легитимный кросс-платформенный fallback, на Ubuntu берётся `.venv/bin/python`)_

**Как сделано:** `scripts/update.sh` (POSIX entrypoint: venv-детект Ubuntu/Windows, лок атомарным
`mkdir` — переносимее `flock`, лог в `logs/update.log`, проброс кода возврата) зовёт
`run --ftp --skip-known`. Skip-если-уже-применён (`run._uzhe_primenen` по `price_meta.file_name` —
без скачивания). systemd `deploy/telewin-update.{service,timer}` (oneshot + `OnCalendar` раз в сутки,
пути-плейсхолдеры) + cron-альтернатива; runbook `docs/DEPLOY_UPDATE.md`. Точка алерта — ненулевой код
+ `ERROR` (`OnFailure=`/healthcheck, вне скоупа). Всё переносимо: `os.path`/`pathlib`, креды из `.env`.

---

# Milestone 5 — Продакшн: мультиканал (TG+VK+MAX) + деплой на сервер (этапы 25–31)

**Мотивация.** Milestone 1–4 дали рабочий гибридный поиск и самообновляемый каталог, но **всё
крутится локально и только в Telegram** (@NikaAi1Bot, обкаточный канал). Клиенту нужны **три канала**:
Telegram (тест), **VK** (сообщество «Домашний мастер», Bots Long Poll) и **MAX** (бот
`id420320118936_bot`, Long Polling). Ядро диалога (`run_agent`) уже каналонезависимо — принимает и
отдаёт чистый текст; Telegram-специфична лишь обвязка в `main.py`. Эта веха: извлечь ядро, навесить
2 адаптера, довести логи до «каждой щели» со сквозным request-id и **выкатить на прод-сервер**
`87.58.210.46` (Docker с нуля, CI/CD).

**Решения по архитектуре (зафиксированы с заказчиком):**
- **Один процесс на 3 канала** (`asyncio.gather`): индекс поиска (11 864 товара) грузится в память
  ОДИН раз и общий для всех каналов — экономит RAM (сервер ~1 ГБ) и даёт общий request-id-трейс.
- **Весь прод на `87.58.210.46` через Docker** (app + postgres-pgvector + redis в docker-compose),
  Docker ставится с нуля (сейчас на сервере только FTP-приём прайса). Эмбеддинги считаются через
  OpenRouter API (не грузят RAM) — 1 ГБ хватает.
- **Репозиторий публичный:** `github.com/dimatolpygin/telewin_new_python`. Секреты только в `.env`
  (gitignore) и GitHub Secrets, в историю не попадают.

**Инварианты всех этапов Milestone 5:**
- **Поиск не трогаем.** Ядро (`bot/search/*`, `bot/ai/agent.py`) — заморожено; веха про транспорт,
  логи и деплой. Любая правка поиска → отдельная задача с прогоном `baseline_v2`.
- **Сессии разведены по каналам:** Redis-ключ включает канал (`telewin:dialog:{канал}:{chat_key}`),
  иначе VK peer_id 123 схлопнется с TG chat_id 123.
- **Очередь-замок на (канал, чат)** сохраняется во всех каналах — два быстрых сообщения не гонятся
  за одну историю (перенос гарантии из `main.py`).
- Каждый канал **демонстрируем отдельно** живым UAT до оркестрации.

## Этап 25 — Публичный репозиторий + каналонезависимое ядро диалога

**Цель**: Telegram отвечает через извлечённое общее ядро `core.obrabotat(...)`, сессии разведены по
каналам, код виден в публичном репозитории. Фундамент, на который встают VK и MAX.

**Что делаем**:
- подключить remote `github.com/dimatolpygin/telewin_new_python`, запушить ветку `dev`; проверить,
  что в истории нет `.env`/токенов (репо изолирован с этапа 0, `.env` в gitignore — сверить фактом);
- извлечь из `main.py` каналонезависимый сервис `bot/core.py`: `obrabotat(channel, chat_key,
  user_text, *, username, first_name, typing_cb=None) -> Otvet(text, zaprosy_poiska, naydeno)`;
  внутри — замок на `(channel, chat_key)`, чтение даты прайса, `run_agent`, запись сессии;
- namespacing сессий: `Sessions` принимает `channel` → ключ `telewin:dialog:{channel}:{chat_key}`;
- Telegram-адаптер `bot/channels/telegram.py` становится тонким (aiogram → core), `main.py` пока
  поднимает только его;
- общий модуль текстов/команд: приветствие (`WELCOME`) и `/reset` — один источник для всех каналов.

**Критерии приёмки** (закрытие → tag `stage-25-done`):
- [x] `git remote -v` = публичный репо; ветка `dev` запушена; в истории НЕТ `.env`/токенов (проверка `git log --all -p -- .env` пусто + grep по токенам)
      _(`.env` никогда не коммитился; blob-grep по `sk-or-v1-`/`vk1.a.`/`:AAF` — только плейсхолдеры в `.env.example`; ветка + теги `stage-0..24` на `github.com/dimatolpygin/telewin_new_python`)_
- [x] Telegram отвечает через `core.obrabotat` (в `main.py`/адаптере нет прямого вызова `run_agent`); регресса TG нет (живой UAT: 3 запроса дают прежние ответы)
      _(живой UAT человеком: `/start`, «гвозди сотка», «саморезы для гипсокартона», «чем прикрутить профлист», `/reset` — ответы как прежде; `grep run_agent` по транспорту пусто)_
- [x] ключ сессии содержит канал (`telewin:dialog:telegram:<id>` в Redis); `/reset` чистит только свой канал
      _(`_key(channel, id)` → `telewin:dialog:{канал}:{id}`; проверено: tg/vk/max с одним id дают разные ключи)_
- [x] очередь-замок на `(канал, чат)` сохранена — два быстрых подряд сообщения обрабатываются по порядку
      _(автотест конкурентности: два `obrabotat` в один чат не чередуются — `[start A, end A, start B, end B]`)_
- [x] приветствие и `/reset` — общий модуль, не продублированы в адаптере
      _(`bot/texts.py`: WELCOME/RESET_OK/ERROR_RETRY — один источник для всех каналов)_

**Как сделано:** доменная логика извлечена из `main.py` в `bot/core.py` (`Yadro.obrabotat(channel,
chat_key, user_text, typing_cb=)` — замок на `(канал, чат)`, дата прайса из `price_meta`, `run_agent`,
сессия). Транспорт Telegram — тонкий `bot/channels/telegram.py` (aiogram → ядро). `bot/session.py`
разводит историю по каналам (ключ с каналом). `main.py` собирает пул/поиск/гибрид/ядро один раз
(индекс общий для будущих VK/MAX) и поднимает канал. Тексты — общий `bot/texts.py`. Репо
опубликован (`origin` = публичный GitHub, `dev` + теги запушены).

## Этап 26 — Слой логов «каждая щель» + сквозной request-id

**Цель**: по одному запросу в логах видна вся цепочка — вход → tool-call'ы → вызов ИИ
(латентность/токены) → исходящий — сшитая ОДНИМ request-id; ошибки с полным контекстом.

**Что делаем**:
- request-id генерится на каждое входящее сообщение, прокидывается через `core → agent → search →
  OpenRouter` (через `contextvars.ContextVar`, чтобы не таскать параметром);
- логировать: входящее (канал, user, текст); каждый tool-call (имя + аргументы + сколько найдено);
  каждый вызов ИИ (модель, латентность мс, токены prompt/completion при наличии `usage`); исходящий
  ответ; лиды (если появятся); ошибки — `ERROR` с traceback + request-id + канал + исходный текст;
- каждая строка лога содержит канал и request-id; вывод в stdout (для Docker) + опционально файл;
- уровень `LOG_LEVEL`: на `debug` добавляются промпт/ответ ИИ, на `info` — нет (не течёт в прод).

**Критерии приёмки** (закрытие → tag `stage-26-done`):
- [x] один запрос → в логах цепочка с ОДНИМ request-id: вход, ≥1 tool-call (аргументы+результат), вызов ИИ (латентность), исходящий
      _(автотест: `👤`→`🔧 tool`→`🧠 ИИ …мс`→`🤖` под одним 8-символьным rid; два конкурентных запроса не смешиваются)_
- [x] каждая строка лога несёт канал (`telegram`/`vk`/`max`) и request-id
      _(`_KontekstFilter` подмешивает `[канал rid]` в каждую запись через contextvars per-task)_
- [x] ошибка в обработке → строка `ERROR` с traceback + request-id + канал + исходный текст запроса
      _(`log_oshibka(..., zapros=)` → `❌ … | запрос: '…'` + `exc_info` traceback + `[канал rid]`)_
- [x] вызов ИИ логирует модель + латентность (+ токены, если OpenRouter вернул `usage`)
      _(`openrouter.chat`: `🧠 ИИ {model} · {ms}мс · токены p+c=t`; латентность вокруг POST)_
- [x] `LOG_LEVEL=debug` добавляет промпт/ответ ИИ; на `info` их нет
      _(автотест: `info` → 0 строк с промптом; `debug` → `🧠 промпт:`/`🧠 ответ:`)_

**Как сделано:** `bot/logger.py` — contextvars `_channel`/`_rid` + `_KontekstFilter` подмешивает их
в КАЖДУЮ строку (формат `[канал rid]`), поэтому id не таскается параметром через
`core → agent → openrouter`. `nachat_zapros(channel)` (contextmanager) открывает контекст запроса
с новым rid — зовётся адаптером на входящем сообщении; contextvars per-task → конкурентные запросы
не смешиваются. Помощники `log_vhodyashchee/tool_call/vyzov_ii/ishodyashchee/oshibka`. Латентность и
токены — в `openrouter.chat` (вокруг POST, `usage` из ответа). `log_tool_call` — после каждого поиска
в `agent.py`. Гейт `DEBUG_LOGS` (LOG_LEVEL=debug) включает промпт/ответ ИИ.

## Этап 27 — Канал VK (Bots Long Poll)

**Цель**: сообщение сообществу «Домашний мастер» в ВК → бот отвечает ценой/наличием через общее ядро.

**Что делаем**:
- VK-адаптер `bot/channels/vk.py`: **Bots Long Poll** (токен `VK_TOKEN` из env, сообщество `dmberez`
  id 214164686); `message_new` → `core.obrabotat(channel="vk", chat_key=peer_id, …)` → `messages.send`
  (с `random_id`);
- команды: первое сообщение / «начать» → welcome; «сброс» → `/reset`-эквивалент;
- устойчивость: обновление `ts`/`key` при `failed`, ретраи к VK API, переподключение longpoll без
  падения процесса.

**Критерии приёмки** (закрытие → tag `stage-27-done`):
- [x] запрос в ВК-сообществе → ответ бота с ценой/наличием (живой UAT человеком)
      _(UAT 23:29–23:30: «есть ли пихтовое масло?» → бот выдал 2 позиции с ценами 251/438 руб и остатками по точкам; связка groups.getById→dmberez, getLongPollServer, a_check проверена живьём)_
- [x] сессия ВК изолирована (ключ `…:vk:<peer_id>`), не пересекается с TG
      _(core → `Sessions` с channel="vk"; ключ `telewin:dialog:vk:<peer_id>`)_
- [x] longpoll переподключается при обрыве (`failed:1/2/3`) без падения процесса
      _(failed:1 → сдвиг ts; failed:2/3 → перезапрос key(+ts); TransportError/Timeout → пауза 1с + continue)_
- [x] лог ВК-запроса несёт request-id и канал `vk`
      _(UAT-лог: `[vk a27149e1]`/`[vk 0722f512]` — цепочка 👤→🧠→🔧→🧠→🤖 под одним rid на запрос)_

**Как сделано:** `bot/channels/vk.py` — raw httpx Bots Long Poll (без `vkbottle`, чтобы чисто встать
в `asyncio.gather` этапа 29). `groups.getLongPollServer` → цикл `act=a_check`; `message_new` →
`yadro.obrabotat("vk", peer_id, …)` → `messages.send` (random_id). Welcome по «начать»/payload start,
сброс по «сброс». Устойчивость: `failed:1` сдвигает ts, `failed:2/3` перезапрашивает key(+ts), сетевые
ошибки — пауза+переподключение. Каждое сообщение — своя задача (свой request-id); очередь на
(канал, peer) держит ядро. `bot/bootstrap.py::sozdat_yadro` собирает поиск/ядро один раз (общее для
всех каналов); `python -m bot.channels.vk` — standalone для UAT.

## Этап 28 — Канал MAX (Long Polling, Authorization-заголовок)

**Цель**: сообщение MAX-боту → ответ через ядро. Критично: токен передаётся **заголовком**
`Authorization`, а не `?access_token=` (query-параметр устарел, API отвечает `verify.token`).

**Что делаем**:
- MAX-адаптер `bot/channels/max.py`: **Long Polling** через httpx, токен `MAX_TOKEN` в заголовке
  `Authorization`; `getUpdates` (long poll) → `message_created` → `core.obrabotat(channel="max", …)`
  → `sendMessage`;
- welcome/сброс аналогично; ретраи и переподключение при таймауте/ошибке.

**Критерии приёмки** (закрытие → tag `stage-28-done`):
- [x] запрос MAX-боту → ответ (живой UAT человеком)
      _(UAT 23:44–23:45: «есть сальники?»→уточнение, «для крана»→таблица 7 размеров с ценами 6,50–7,00 ₽ и остатками; контекст диалога держится)_
- [x] токен передаётся заголовком `Authorization` (не query) — подтверждено рабочим ответом API
      _(GET /me → 200, бот «Домашний мастепр» id420320118936_bot user_id 364106946; `_MaxApi` шлёт `Authorization: <токен>`)_
- [x] сессия MAX изолирована (`…:max:<chat_id>`)
      _(core → `Sessions` с channel="max"; ключ `telewin:dialog:max:<chat_id>`)_
- [x] long polling переподключается при таймауте/ошибке; лог несёт request-id и канал `max`
      _(TransportError/Timeout → пауза 1с; HTTPStatusError → пауза 3с; UAT-лог `[max c5332c66]`/`[max f23d5b2b]` цепочка под rid)_

**Как сделано:** `bot/channels/max.py` — raw httpx Long Polling к `platform-api2.max.ru` (не старый
`botapi`). Токен заголовком `Authorization` (query устарел). `GET /updates?marker=&timeout=` →
`{updates, marker}`; marker-курсор; слив стартового marker (после рестарта не переотвечаем на старые).
`message_created`/`bot_started` → `yadro.obrabotat("max", chat_id, …)` → `POST /messages?chat_id=`.
**TLS:** сервер под сертификатом Минцифры — bundle certifi ⊕ Минцифры (`certs/russian_trusted_{root,
sub}_ca.cer`, публичные CA в репо). Каждое сообщение — своя задача (свой request-id); очередь на
(канал, chat) держит ядро. Standalone: `python -m bot.channels.max`.

_Косметика на будущее (не в скоупе):_ ответы содержат markdown (таблицы/`**жирный**`) — TG рендерит
не всё, VK/MAX показывают сырьём. Форматирование под каналы — отдельная задача.

## Этап 29 — Оркестрация трёх каналов в одном процессе

**Цель**: один `python -m bot.main` поднимает TG+VK+MAX поверх ОБЩЕГО индекса поиска в памяти;
корректное завершение по сигналу; падение одного канала не рушит остальные.

**Что делаем**:
- `main.py`: конфиг/пул/поиск/гибрид грузятся ОДИН раз, затем `asyncio.gather(tg, vk, max)`;
- включение каналов флагом `ENABLED_CHANNELS` (или наличием токена) — можно поднять подмножество;
- graceful shutdown: `SIGTERM`/`SIGINT` → отмена задач каналов, закрытие redis/pool/сессий;
- супервизор канала: исключение в одном канале → лог + контролируемый рестарт его задачи, остальные
  живы (не молчаливое падение).

**Критерии приёмки** (закрытие → tag `stage-29-done`):
- [x] один процесс поднимает все 3 канала; лог «Поиск готов: N товаров» ровно ОДИН раз (индекс общий)
      _(UAT 23:56: `Поиск готов: 11864` один раз, затем telegram @NikaAi1Bot / vk группа 214164686 / max id420320118936_bot; три канала ответили параллельно каждый со своим request-id)_
- [x] можно поднять подмножество (нет `MAX_TOKEN` → MAX выключен, TG+VK работают)
      _(адаптеры возвращаются сразу без токена; автотест: выключенный канал завершается штатно, не мешает остальным)_
- [x] `SIGTERM` → корректное закрытие (redis/pool/сессии закрыты, нет висящих корутин в логе)
      _(автотест `main()`: при остановке `sessions.zakryt()` + `pool.close()` вызваны; отмена задач через gather return_exceptions)_
- [x] падение одного канала (симулированное исключение) не убивает остальные; в логе — рестарт задачи
      _(автотест супервизора: crash-канал перезапущен 3× с логом+traceback, healthy-канал крутился нетронутым, отмена чистая)_

**Как сделано:** `bot/main.py` — `sozdat_yadro(cfg)` грузит поиск/ядро ОДИН раз (общий индекс), затем
`asyncio.gather` трёх каналов, каждый в задаче под `_supervise` (штатное завершение = канал выключен;
падение → лог+traceback+перезапуск через `_RESTART_PAUZA`, соседи не задеты; `CancelledError`
пробрасывается для shutdown). `_ustanovit_signaly` вешает SIGINT/SIGTERM → `stop.set()` (best-effort;
Windows — `KeyboardInterrupt` в `__main__`). Ждём сигнал ИЛИ завершение всех каналов, затем отмена
задач + `sessions.zakryt()`/`pool.close()`. Список `KANALY` — добавить канал = одна строка.

## Этап 30 — Контейнеризация: Dockerfile + docker-compose (app + pgvector + redis)

**Цель**: `docker compose up` локально поднимает весь стек (app с 3 каналами + postgres-pgvector +
redis); первичная инициализация схемы/прайса/эмбеддингов воспроизводима.

**Что делаем**:
- `Dockerfile` (python-slim, `requirements.txt`, non-root, entrypoint под asyncio-процесс);
- `docker-compose.yml`: `app` + `postgres` (образ с pgvector) + `redis`; volume под pgdata;
  healthcheck БД; `app` ждёт готовности БД;
- инициализация чистого volume: расширение `pgvector`, схема, `import_price`, эмбеддинги
  (`embed_index --all`, ~35 мин/деньги — задокументировать порядок; отдельный one-off шаг);
- `.env.example` дополнить: `VK_TOKEN`, `MAX_TOKEN`, `ENABLED_CHANNELS`, `LOG_LEVEL`;
- автообновление прайса (M4-таймер) — уложить в контейнеризованный вариант (cron/таймер) и проверить.

**Критерии приёмки** (закрытие → tag `stage-30-done`):
- [x] `docker compose up` локально поднимает app+pg(pgvector)+redis; бот отвечает в TG (тестовый канал)
      _(живой UAT человеком: `docker compose up` → «Поиск готов: 11864» один раз + 3 канала; TG/VK/MAX ответили из контейнера на «тест»)_
- [x] схема+прайс инициализируются в чистом volume воспроизводимо (порядок задокументирован, вкл. эмбеддинги)
      _(initdb → extension+схема; `import_price` → 11864 строк + столбец embedding; runbook `docs/DEPLOY_DOCKER.md` с порядком вкл. платные эмбеддинги)_
- [x] в контейнере БД поднимается `pgvector` и столбец `halfvec(3072)` (HNSW `products_embedding_hnsw` строит `embed_index`)
      _(`select extname` → vector; `\d products` → `embedding halfvec(3072)`; HNSW — на шаге эмбеддингов из runbook)_
- [x] `.env.example` покрывает все новые переменные; секреты не зашиты в образ
      _(VK_TOKEN/VK_GROUP_ID/MAX_TOKEN/LOG_LEVEL; `.env` в `.dockerignore` — токены через env_file, не в слои образа)_
- [x] автообновление прайса (M4) проходит в контейнеризованном варианте (таймер/cron) — цикл ok
      _(`docker compose run --rm updater` → FTP→validate→upsert(11864 без изменений)→монитор, exit 0, 16.7с; host-cron в runbook)_

**Как сделано:** `Dockerfile` (python:3.12-slim, requirements слоем, non-root, CMD `bot.main`).
`docker-compose.yml`: `db` (pgvector/pgvector:pg16, `docker/initdb/01-extension.sql` → extension+схема
на чистом volume, healthcheck, том pgdata) + `redis` (том redisdata) + `app` (env_file `.env`,
PGHOST/PGPORT/REDIS_URL переопределены на сервисы compose, ждёт healthy БД, том `./data`) + `updater`
(profile `tools`, одноразовый цикл обновления для host-таймера). `import_price` заводит расширение и
столбец `embedding halfvec(3072)` В КОДЕ (раньше вручную → деплой был невоспроизводим) и починен
латентный баг: COPY писал NULL в serial `id` (в products.json id нет) → id исключён из COPY. Runbook
`docs/DEPLOY_DOCKER.md`.

## Этап 31 — Деплой на 87.58.210.46 + CI/CD (через `okdeploy`)

**Цель**: пуш в `master` → GitHub Actions автодеплой → на сервере живы 3 канала + автообновление
прайса. Docker ставится на сервер с нуля, FTP-приём не ломается.

**РЕШЕНИЯ (согласованы с заказчиком 2026-07-21):**
- **Эмбеддинги НЕ пересчитываем на проде — переносим готовые векторы из локальной БД.** Локальный
  `telewin-test-pg` (:5434) уже держит 11864/11864 с `embedding halfvec(3072)` + HNSW. План: `pg_dump`
  таблицы `telewin_test.products` (DDL + данные вместе с `embedding` + индексы) → восстановить на
  сервере после `CREATE EXTENSION vector`. **Экономит ~35 мин и кредиты OpenRouter, деплой стартует
  сразу с полным гибридом (95% золото).** Первичный `import_price`/`embed_index --all` на проде НЕ
  нужен — только восстановление дампа (`embed_index --index` при необходимости пере-собрать HNSW,
  без обращений к API).
- **CI/CD — во вторую очередь.** Сначала РУЧНОЙ деплой по SSH + проверка что всё работает, ЗАТЕМ
  настройка GitHub Actions (создать `master` + workflow) и выдача списка Secrets заказчику.

**Что делаем** (через `/okdeploy`):
- подготовка сервера: Docker+compose с нуля (swap уже есть), firewall, SSH deploy-ключ; не сломать FTP;
- клон публичного репо, `.env` на сервере (токены, PG=localhost-контейнер, OpenRouter, FTP —
  уже на этом же сервере); поднять db+redis;
- **перенос данных:** `pg_dump` локальной `products` (с векторами) → restore на серверную БД
  (вместо `import_price` + `embed_index --all`);
- запустить app (3 канала), проверить прод; **потом** GitHub Actions: `master` → ssh → `git pull` →
  `docker compose up -d --build`, список Secrets — человеку.

**Критерии приёмки** (закрытие → tag `stage-31-done`):
- [x] на сервере `docker compose ps` = app+pg+redis healthy; 3 канала отвечают (живой UAT в TG, VK, MAX)
      _(«Поиск готов: 11864» один раз + telegram/vk/max подключены; живой UAT человеком — 3 канала ответили ценой/наличием)_
- [x] Docker установлен на `87.58.210.46` с нуля; FTP-приём прайса продолжает работать (сосуществуют)
      _(Docker 29.6.2 + Compose v5.3.1 с нуля; `vsftpd` active на :21 после установки — не задет; compose хост-порты не публикует)_
- [x] пуш в `master` → Actions деплоит без ручных шагов на сервере; логи деплоя зелёные
      _(тест-коммит `4c859c5` → Actions → сервер на `4c859c5`, маркер `сборка 2026-07-22.1` в логах, векторы целы через пересборку)_
- [x] сквозная проверка автообновления на проде: файл на FTP → подхватился сам → бот по свежему каталогу
      _(`docker compose run --rm updater` из контейнера достаёт FTP (hairpin NAT ok) → validate→upsert→monitor, exit 0, 11864/11864; host-cron 04:00)_
- [x] секреты только в GitHub Secrets / `.env` на сервере (не в репо); список Secrets передан человеку
      _(SSH_PRIVATE_KEY/SERVER_* в GitHub Secrets; `.env` на сервере chmod 600, untracked; PG-пароль свежий; репо чист)_

**Как сделано:** ручной SSH-деплой через `okdeploy` (`okdeploy`-навык): ed25519 deploy-ключ →
`authorized_keys`; Docker+compose установлены с нуля (get.docker.com), `vsftpd` не тронут; репо
склонирован в `/opt/telewin/tgbot_py`; `.env` залит через scp (свежий PG-пароль, токены TG/VK/MAX/
OpenRouter/FTP). **Перенос векторов вместо пересчёта:** `pg_dump -Fc -t telewin_test.products`
(154 МБ, 11864 строки с `embedding halfvec(3072)` + все 4 индекса вкл. HNSW) → `pg_restore` на сервер
после `CREATE EXTENSION vector` (initdb) — прод стартовал сразу с гибридом 95%, `import_price`/
`embed_index --all` НЕ запускались (0 обращений к OpenRouter, ~35 мин и кредиты сэкономлены). Локальный
поллер остановлен (конфликт `getUpdates` по общему `BOT_TOKEN`). **CI/CD:** `.github/workflows/deploy.yml`
(appleboy/ssh-action, push `master` → `git reset --hard origin/master` + `docker compose up -d --build` +
`image prune`; том `pgdata` с векторами переживает деплой, untracked `.env` не затрагивается); ветка
`master` создана, сервер переведён на неё; пайплайн проверен тест-коммитом (зелёный). **Фикс деплоя:**
`chown 1000:1000 docs logs` — контейнер под non-root `app` не мог писать отчёты монитора в смонтированные
root-owned каталоги. Host-cron `0 4 * * *` — ежедневное автообновление прайса.

---

# Milestone 6 — Живое общение (человечный стиль ответов) (этап 32)

**Мотивация.** Поиск и работа с прайсом отлажены (Milestone 1–5, золото 95%), но САМ СТИЛЬ
ответов роботизирован: бот вываливает выдачу простынёй с markdown-жирным, заголовками и
маркированными списками, а в конце робо-подвал «Данные актуальны на 18.07.2026». Пример живого
провала (запрос «саморезы 50 для профлиста к дереву»): ответ на 15 строк с `**жирным**`,
буллетами `-`, вложенными списками по остаткам и подписью про дату. Покупатель сразу понимает,
что общается с ботом. Нужен стиль живого продавца-консультанта: коротко, своими словами, по сути,
без форматирования. **Референс правильного стиля** — соседний бот `Downloads/vk0043` (файлы
`knowledge/rules.md` «Как ты общаешься» + `humanize.py` чистка тире): короткие реплики, без markdown,
без «—», без канцелярита, приветствие один раз, не вываливать весь прайс, обучение на АНТИпримерах.

**Границы вехи (жёстко):**
- **Поиск/прайс НЕ трогаем.** `bot/search/*`, гибрид, tool-calling `search_products`, брендовая
  пометка, предохранитель отказа, payload инструмента — заморожены. Веха только про СТИЛЬ ответа.
- Решения по стилю (согласовано с заказчиком): **персона с именем** (живой консультант «Сергей» из
  «Домашнего мастера», покупатель не должен догадаться, что это бот); **глубина — только стиль текста**
  (короткие живые ответы + постобработка markdown/тире), БЕЗ дробления на несколько реплик и пауз
  набора (это транспортный слой, отложено).
- **Правило заказчика по чистоте:** тире «—» и markdown убираются ПОСТОБРАБОТКОЙ (regex в коде),
  не промтом. Промт отвечает за тон/краткость/поведение, код — за гарантию чистоты (LLM всё равно
  иногда проставит «—»/`**`).

## Этап 32 — Человечный стиль ответов (промт + постобработка)

**Цель**: бот отвечает как живой консультант Сергей — коротко, по делу, своими словами, без
markdown/списков/тире/канцелярита/робо-подвала; на тот самый запрос про саморезы отвечает
по-человечески. Поиск и цифры не меняются.

**Что делаем**:
- переписать `SYSTEM` в `bot/ai/agent.py`: (1) **персона** — Сергей, живой продавец-консультант
  сети «Домашний мастер», не бот/не менеджер/не ассистент; (2) блок **«Как ты общаешься»** —
  отвечай как человек в мессенджере: коротко (обычно 1–2 предложения), без markdown (без `**жирного**`,
  без заголовков `#`, без маркированных/нумерованных списков), без длинного тире «—» (только запятая/
  двоеточие/точка), без канцелярита («осуществляется», «в наличии имеется», «в кратчайшие сроки»),
  без эмодзи, приветствие и представление РОВНО один раз в начале диалога; не вываливать весь прайс —
  рекомендовать по сути (обычно один лучший вариант + при желании альтернатива), цену/остаток называть
  по конкретной позиции; дату прайса называть ТОЛЬКО если спросили или это уместно, не подвалом на
  каждый ответ; (3) **антипримеры** на нашем домене (робо-выгрузка саморезов «плохо» vs короткий
  человеческий ответ «хорошо») — по референсу, антипример работает лучше инструкции;
- **убрать** из промта строку «Всегда добавляй, что данные актуальны на {ДАТА}» (источник робо-подвала);
- **постобработка ответа** (код, `bot/ai/`): функция очистки применяется к `answer` в `run_agent`
  перед возвратом — убирает «—»/«–» (диапазоны чисел `10—15`→`10-15`, тире между словами→запятая),
  снимает markdown-разметку (`**bold**`→текст, заголовки, маркеры списков `-`/`*`/`•` и нумерацию в
  начале строк), схлопывает лишние пустые строки. Переиспользовать подход `vk0043/humanize.py::strip_ai_dashes`;
- НЕ трогать: `search.py`/`gibrid.py`, TOOLS, tool-calling цикл, `brend_iz_zaprosa`/`примечание_бренд`,
  предохранитель `_zayavil_otkaz`, структуру payload.

**Критерии приёмки** (закрытие → tag `stage-32-done`):
- [ ] запрос «саморезы 50 для профлиста к дереву» → короткий человеческий ответ (рекомендация по сути,
      цена/наличие по конкретной позиции), БЕЗ `**жирного**`, БЕЗ буллет-списков, БЕЗ «—», БЕЗ подписи
      «данные актуальны на …» по умолчанию (живой смоук в TG/VK/MAX)
- [ ] постобработка гарантирует чистоту: в готовом ответе нет символов «—»/«–», нет `**`/`#`/маркеров
      списка в начале строк (автопроверка на наборе типовых ответов)
- [ ] поведение поиска НЕ изменилось: бот по-прежнему вызывает `search_products` и называет РЕАЛЬНЫЕ
      числа из прайса (не выдумывает); `baseline_v2` идентичен (промт не влияет на ранжирование —
      тракт поиска не тронут); уточнение на родовых, честный отказ на непрофильном, брендовая пометка — сохранены
- [ ] приветствие/представление один раз (во втором сообщении диалога бот не представляется заново)

**Примечание:** имя «Сергей» — плейсхолдер, меняется одной строкой в промте. Дробление ответа на
несколько коротких реплик с паузами набора (полный «живой чат» как в референсе) — возможная следующая
веха (транспортный слой каналов), в скоуп этапа 32 не входит по решению заказчика.
