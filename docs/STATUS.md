# STATUS — Python-порт бота (ИИ-поиск по прайсу «Домашний мастер»)

> **Для Claude и человека**: это первый файл, который читается в новой сессии. После
> прочтения **обязательно** сверить с реальностью: `git tag -l "stage-*"`,
> `git log --oneline -20` — таблица ниже может быть устаревшей.

**Последнее обновление**: 2026-07-21 (Milestone 3 закрыт; нарезан **Milestone 4** — автообновление из FTP по расписанию, готовое к Ubuntu)
**Текущий этап**: 🚧 **этап 24 — планировщик (cron/systemd, Ubuntu-ready)** (Milestone 4). Этапы 17/18 собраны, ждут верификации (теги по подтверждению). 24 — последний в вехе.
**Следующий шаг**: закрыть 24 → Milestone 4 завершён. Не забыть теги 17/18/24.

---

## Сводка этапов

Легенда: ☐ не начат · 🚧 в работе · ✅ закрыт · ⏸ заблокирован

| # | Этап | Статус | Закрывающий тег | Последний коммит | Дата закрытия |
|---|---|---|---|---|---|
| 0 | Каркас Python-проекта | ✅ | `stage-0-done` | `c9fd413` | 2026-07-20 |
| 1 | Ядро поиска (паритет 92%) | ✅ | `stage-1-done` | `e02b131` | 2026-07-20 |
| 2 | Слой данных Postgres | ✅ | `stage-2-done` | `24b3fee` | 2026-07-20 |
| 3 | ИИ-оркестрация | ✅ | `stage-3-done` | `ac297ec` | 2026-07-20 |
| 4 | Telegram-слой | ✅ | `stage-4-done` | `fb16774` | 2026-07-20 |
| 5 | Улучшение поиска + замер | ✅ | `stage-5-done` | `7761b90` | 2026-07-20 |
| 6 | Тестирование субагентами | ✅ | `stage-6-done` | `a57cbfd` | 2026-07-20 |
| 7 | Материалы-в-имени + порог soft_has | ✅ | `stage-7-done` | `45a174a` | 2026-07-20 |
| 8 | pg_trgm фаззи-канал (опечатки) | ✅ | `stage-8-done` | `b6d470f` | 2026-07-20 |
| 9 | Головное слово + длины см/м | ✅ | `stage-9-done` | `c9c0cb6` | 2026-07-20 |
| 10 | Гибрид pgvector + RRF | ✅ | `stage-10-done` | `594d986` | 2026-07-20 |
| — | **Milestone 2 — Апгрейд качества поиска** | | | | |
| 11 | Мерная линейка: тест-набор + baseline | ✅ | `stage-11-done` | `14f09c0` | 2026-07-21 |
| 12 | Абстейн (риск №1) 34%→91% | ✅ | `stage-12-done` | `be8455a` | 2026-07-21 |
| 13 | Словарь народных названий, синонимов, кодов | ✅ | `stage-13-done` | `eaa520a` | 2026-07-21 |
| 14 | Атрибуты: дюймы, цоколь, размеры, материал | ✅ | `stage-14-done` | `3a01e2f` | 2026-07-21 |
| 15 | Бренды: чистка «производителя», канал, отказ | ✅ | `stage-15-done` | `70dfb8e` | 2026-07-21 |
| 16 | Категорийные каналы для родовых + уточнение | ✅ | `stage-16-done` | `3a6b390` | 2026-07-21 |
| — | **Milestone 3 — Ежедневное обновление данных из FTP** | | | | |
| 17 | Приём файла с FTP + валидация до обработки | ☐ → M4 | — | — | — |
| 18 | Парсер прайса с автоопределением раскладки | ☐ → M4 | — | — | — |
| 19 | UPSERT-diff вместо DROP (вектор только при смене имени) | ✅ | `stage-19-done` | `d11cdfc` | 2026-07-21 |
| 20 | Инкрементальный re-embed + дата актуальности | ✅ | `stage-20-done` | `d2218ca` | 2026-07-21 |
| 21 | Монитор новизны: новые семейства → сигнал на словарь | ✅ | `stage-21-done` | `37d70d9` | 2026-07-21 |
| 22 | Оркестрация ежедневного цикла + прод-прогон | ✅ | `stage-22-done` | `7bd1091` | 2026-07-21 |
| 23 | Трекер новых семейств (`novelty_families`) + история | ✅ | `stage-23-done` | `fefce9a` | 2026-07-21 |
| — | **Milestone 4 — Автообновление из FTP по расписанию (Ubuntu-ready)** | | | | |
| 17 | Приём файла с FTP + валидация *(реактивирован)* | 🚧 (на верификации) | — | — | — |
| 18 | Парсер с автоопределением раскладки 8↔11 *(реактивирован)* | 🚧 (на верификации) | — | — | — |
| 24 | Планировщик ежедневного цикла (cron/systemd, Ubuntu-ready) | 🚧 | — | — | — |

Детальные критерии приёмки каждого этапа — в [`ROADMAP.md`](ROADMAP.md).

## Активная работа

Нет активной работы. **Milestone 3 завершён** (этапы 19–23 закрыты; 17/18 пропущены).

**Milestone 4 — Автообновление из FTP по расписанию (в работе).** Замыкает «файл на FTP →
подхватился сам → бот по свежему каталогу». Три этапа: **17** (FTP-приём) — ✅ собран, ждёт
верификации; **18** (автораскладка 8↔11) и **24** (планировщик cron/systemd) — впереди.

**Этап 17 собран** (`bot/update/istochnik.py`): абстракция источника `poluchit_svezhiy()` —
`FtpIstochnik` (ftplib, свежий по дате в имени, размер-гейт `SIZE` ДО скачивания, загрузка в
`tempfile`-папку) + `LokalnyyIstochnik`. Флаг `--ftp` в `run.py`, `FtpConfig`/`PRICE_FTP_*` в `.env`.
Проверено на **реальном FTP Форы** (`87.58.210.46`): скачал боевой 3.17 МБ (MD5 == локального),
полный цикл идемпотентен exit 0; размер-гейт отклоняет пустышку ДО скачивания (exit 2, БД цела);
локальный источник тем же интерфейсом; Windows-специфики в тракте нет; БД/вектора не тронуты.

**Этап 18 собран** (`bot/update/parser.py`): `opredelit_raskladku` по числу колонок (11/8, иначе
`ParserError`); внутри 11-кол — какая колонка штрихкод решается ПО СОДЕРЖИМОМУ (в образце 06.07
артикул↔штрихкод стояли наоборот). `sync._prochitat` = `parser.prochitat`, `validate` ослаблен до
≥8 колонок. Проверено: 11-кол (11 864, штрихкод 100%, категории 99%), 8-кол (11 888, категории «»,
не падает), 9-кол → `ParserError` ДО БД; идемпотентный `run` на 18.07 без регресса, хэш векторов тот же.

Всё пишется **переносимо под Ubuntu**, на сервер пока ничего НЕ переезжает. Сам деплой
(бот+БД+воркер на сервер) — отдельная будущая веха. Критерии — в `ROADMAP.md` (раздел Milestone 4).

**Этап 22 закрыт** (`stage-22-done`, 7bd1091). Собран идемпотентный воркер
`bot/update/run.py` (`fetch → validate → parse → upsert → embed → monitor → дата`) + гейт
`bot/update/validate.py` (размер/строки/колонки ДО записей). Прогоны на живой БД:
- **пустышка 51 200 Б** → `ОТМЕНА … вероятно пустышка FTP`, exit 2, БД не тронута ✅
- **идемпотентно на боевом 18.07** → 0 вставок/правок/удалений, re-embed no-op, exit 0 ✅
- **дельта на фикстуре 19.07** → 2373 факта / 3 новинки → re-embed 3 через OpenRouter /
  монитор поймал новые семейства Гигрометр, Пирометр / дата 19.07, exit 0 ✅; затем откат на 18.07
- **вектора целы**: MD5 колонки `embedding` до/после полного цикла **идентичен** (`850577…`) ✅
- **baseline_v2**: этап 22 — чистая оркестрация, тракт поиска не тронут (файлы `run.py`/`validate.py`
  не импортируются поиском), эмбеддинги побайтно те же → регресс от этапа 22 исключён по построению.
  Живой прогон baseline: золото top-1 35/40, hit@5 98% (замороженный этап-11 — 38/40); разброс 35–38/40 =
  недетерминизм приближённого вектор-канала (HNSW approx + эмбеддинг запроса каждый раз), не эффект этапа 22.

FTP-приём (этап 17) и автораскладка (этап 18) по-прежнему ПРОПУЩЕНЫ — точка подключения FTP
оставлена в `run._poluchit_fail`; парсер 11-кол локальный, для боевого прогона ок.

**Этап 23 закрыт** (`stage-23-done`, fefce9a). Трекер
`novelty_families` встроен в `bot/update/monitor.py` (таблица: семейство pk, first_seen,
last_seen, позиций, пример, статус) — рабочий список «по чему писать словарь» с АВТО-закрытием,
когда семейство появляется в словаре. Решено трекать ПО СЕМЕЙСТВАМ, не дублировать товары
(рассинхрон цен/остатков). Прогоны на живой БД (фикстур 19.07 → откат 18.07):
- **занос новинки**: Гигрометр/Пирометр → 2 строки с first_seen/счётчиком/примером, статус `new` ✅
- **без дублей**: повтор — first_seen тот же, last_seen новее, строк по-прежнему 2 ✅
- **авто-закрытие**: Гигрометр внесён в словарь → прогон `авто-закрыто … Гигрометр`, статус `внесено`;
  Пирометр остался `new` ✅
- **worklist**: `NOVELTY.md` — «Рабочий список» (активные с датой) отдельно от «Внесены в словарь»;
  CLI `python -m bot.update.monitor --worklist` ✅
- вектора целы (MD5 `embedding` не изменился), стенд возвращён в чистое 18.07, тестовые строки трекера очищены.

FTP-приём (этап 17) и автораскладка (этап 18) по-прежнему ПРОПУЩЕНЫ — точка подключения FTP
оставлена в `run._poluchit_fail`; парсер 11-кол локальный. После верификации 22/23 Milestone 3
закрыт (кроме 17/18).

---

**Этап 21 закрыт** (`stage-21-done`, 37d70d9): монитор новизны `bot/update/monitor.py`. Новые
семейства = в БД, нет в словаре (шум 0); подгруппы vs `known_podgruppy` (первый прогон — посев).
Отчёт `docs/NOVELTY.md` + WARNING. Флаг `--monitor` в `sync.py`. Проверено на фикстуре:
{Гигрометр, Пирометр} + подгруппа {Измерительный инструмент}, Лампа молчок, откат → 0.

**Этап 20 закрыт** (`stage-20-done`, d2218ca): инкрементальный re-embed (`--apply-embed`, считает
только новые/переименованные) + дата актуальности (`price_meta`, бот отдаёт динамически из имени
файла). На фикстуре: re-embed 3 позиции, 11867/11867, дата 19.07↔18.07. БД в чистом 18.07.

**Этап 19 закрыт** (`stage-19-done`, d11cdfc): UPSERT-diff по ключу-штрихкоду (`bot/update/sync.py`).
Факты всегда, `embedding=NULL` только при смене имени, новые→INSERT, исчезнувшие→DELETE. Хеш всех
векторов идентичен до/после, откат байт-в-байт, идемпотентно.

Нет активной работы по Milestone 2. **Milestone 2 (этапы 11–16) полностью закрыт.** Итоговая сводка по 5
сегментам — `docs/ZAMER_RODOVYE.md`: абстейн 34→91%, разговорный 83%, родовой 89%, размер 84→85%,
размерный-точный 84→91%, золото 95% — все цели достигнуты, регресса нет.

Готовое этапа 15 (к переиспользованию): `data/brendy.json` (страны/заглушки + народные алиасы),
`Poisk.brend_iz_zaprosa`, буст бренда +3 внутри семейства, `примечание_бренд` в payload агента.
Готовое этапа 16: `data/rodovye.json` (стем глагола → семейства-кандидаты), мягкая инъекция
родового канала в `search.py`, правило промпта об уточнении многовариантных родовых.

Остаток по этапу 14 (вне цели, если вернёмся): 3 промаха размерной точности — болт мебельный
vs обычный, одиночный диаметр самореза «4.8», спорный `ждём` ШСГМ vs ШСММ (ревизия набора).

Готовые инструменты/данные: `bot/search/baseline_v2.py` (baseline по 5 сегментам, `--save`),
наборы `data/abstain_nabor.json` (44), `data/razmer_nabor.json` (32), словарь чужого домена
`data/chuzhoy_domen.json` (абстейн-гейт), `probe_batch.py`, `dump_group.py`.

Новые инструменты (готовы к переиспользованию): `bot/search/probe_batch.py` (прогон произвольных
запросов через гибрид → JSON), `bot/search/dump_group.py` (выгрузка среза прайса по группе).
Отдельно: в `bot/ai/agent.py` добавлен предохранитель от ложного «нет в прайсе» без вызова
поиска (баг «масло пихтовое», коммит `af94e1e`) — не этап milestone, а точечный фикс.

Инфра гибрида (этап 10): отдельный pgvector-контейнер `telewin-test-pg` (:5434,
`pgvector/pgvector:pg16`), схема `telewin_test`, колонка `embedding halfvec(3072)` + HNSW.
Бот ходит на :5434 (`.env` PGPORT). Общий `postgres16` (:5432) НЕ трогали — там чужие проекты.
Пересчёт эмбеддингов: `python -m bot.embed_index --all` (нужен `OPENROUTER_API_KEY`).

## Известные блокеры

Нет. Баланс OpenRouter подтверждён (нужен для этапов 3–6). Git — изолированный репо
в `tgbot_py/` (ветка `dev`), чужой корневой `restoran326_bot` не затрагивается.

## История закрытий

- 2026-07-21 — **Этап 21: Монитор новизны (Milestone 3)** — tag `stage-21-done` (37d70d9).
  `bot/update/monitor.py`: после апдейта сигналит, если появились позиции, которые будут плохо
  искаться народным языком. **Новые СЕМЕЙСТВА** = есть в БД, нет в `slovar_svodnyy.json` (707
  ключей; базовый шум 0 — все 706 текущих семейств покрыты). **Новые ПОДГРУППЫ** = относительно
  таблицы `known_podgruppy` (первый прогон — посев без ложных, далее diff+дозапись). Отчёт
  `docs/NOVELTY.md` (перезаписывается) + `logger.warning`. Флаг `--monitor` в `sync.py` (для
  оркестрации этапа 22). **Проверка на фикстуре:** чистая база → молчит (посев 69 подгрупп); фикстур
  (Лампа-ТЕСТ переведён на существующую подгруппу «Лампы») → новые семейства {Гигрометр, Пирометр},
  Лампа НЕ во «новых» (в словаре), новая подгруппа {Измерительный инструмент}; откат → 0 новых.
  Отвечает на исходный вопрос заказчика «новые позиции будут искаться?»: по имени/штрихкоду — сразу;
  по народным синонимам — после словарной правки, на которую и указывает отчёт. Ключевое свойство:
  новое семейство всплывает в отчёте КАЖДЫЙ прогон, пока не внесено в словарь (не теряется). БД
  возвращена в чистое 18.07. Принят пользователем. По его же указанию заведён этап 23 (накопительный
  трекер `novelty_families` — рабочий список «по чему писать словарь»), новый этап в работу не берём.
- 2026-07-21 — **Этап 20: Инкрементальный re-embed + дата актуальности (Milestone 3)** — tag
  `stage-20-done` (d2218ca). (1) Флаг `--apply-embed` в `bot/update/sync.py` после UPSERT зовёт
  `embed_index` в инкрементальном режиме (`where embedding is null`) — считает векторы ТОЛЬКО новым/
  переименованным, HNSW перестраивается через `IF NOT EXISTS`. (2) Дата актуальности — таблица-
  синглтон `price_meta` (`bot/update/meta.py`): `sync` пишет `price_date` из имени файла
  `…YYYY-MM-DDThh-mm-ss…` (fallback mtime) + `updated_at` + `file_name`. Бот отдаёт дату динамически:
  `ДАТА_ПРАЙСА` в `ai/agent.py` → плейсхолдер `{ДАТА}`, `run_agent(data_prajsa=…)` подставляет в промт
  и payload; `main.py` читает на КАЖДЫЙ запрос (свежесть без перезапуска — важно для принципа «не
  выдавать вчерашнее»), `chat.py` при старте. **Проверка на фикстуре:** лог «Считаю эмбеддинги для 3
  позиций» (не 11867), покрытие 11867/11867, HNSW на месте, 3 новинки с вектором; дата 19.07.2026 →
  откат sync(18.07) → 18.07.2026; БД возвращена в чистое 18.07 (11864/11864). Честная поправка:
  новинки нового семейства ищутся по имени/штрихкоду сразу, но перефразированные народные запросы к
  ним промахиваются до словарной правки (семантика отдала «Термостат NTC» на «чем измерить
  температуру») — это диагностирует этап 21. Принят (пользователь перешёл к этапу 21).
- 2026-07-21 — **Этап 19: UPSERT-diff вместо DROP (Milestone 3)** — tag `stage-19-done` (d11cdfc).
  Замена `import_price.py` (DROP+COPY, стирал эмбеддинги → полный re-embed 35 мин) на
  инкрементальный `bot/update/sync.py`. **Ключ diff — штрихкод** (исследование прайса: артикул
  непригоден — 200 дублей, 188 с РАЗНЫМИ именами, Фора переиспользует коды под разные товары;
  штрихкод 100% заполнен и уникален, вкл. внутренние `2000…`). Классификация позиций из файла:
  INSERT (нет ключа) / upd_rename (сменилось имя/семейство/группа/подгруппа → `embedding=NULL`) /
  upd_fact (только цена/остаток/произв/ед) / skip (no-op) / DELETE (нет в новом файле); всё в одной
  транзакции. **Верификация на фикстуре** `samples/ТЕСТ_2026-07-19…xls` (2373 цены +5%, 3 новинки):
  diff 2373/0/3/0/9491; `count(embedding)` 11864→11864; **MD5-хеш всех 11864 векторов идентичен
  до/после** — доказано, что смена цены НЕ триггерит re-embed (расход на эмбеддинги = 0); сверка цен
  20/20 по манифесту; новинки легли с `embedding NULL`. Ветка смены имени (`embedding=NULL`) отдельно
  проверена на контролируемой строке (`upd_rename=1`, вектор обнулён, реальные не тронуты). Откат
  `sync(оригинал 18.07)` вернул БД **байт-в-байт** (хеш `823b88f544d8`, delete=3) — идемпотентность.
  `baseline_v2` не гонялся: регресс исключён байт-идентичностью БД (строже замера). Парсер 11-кол
  пока локальный в модуле — автораскладку даст этап 18. Принят (пользователь перешёл к этапу 20).
- 2026-07-21 — **Этап 16: Категорийный канал для родовых + уточнение (ФИНАЛ milestone 2)** — tag
  `stage-16-done` (3a6b390). **Проблема:** родовые действием не резолвились, когда стем глагола ≠
  стему семейства — «чем красить» (`крас`) не находил Краску (`краск`) → пусто; так же «чем
  прикрутить»/«чем приклеить». **Решение (1) канал родовых** `data/rodovye.json`: стем глагола →
  семейства-кандидаты (крас/покрас→Краска/Эмаль/Валик/Кисть, прикрут→Шуруп/Дюбель/Болт, прикле/скле→
  Клей, грунтова→Грунт/Грунтовка, штукатур/шпаклева→Шпатлевка). Мягкая инъекция в `search.py` (вес
  1.0, конкурирует, не короткозамыкает); сверлить/пилить не вносили (уже резолвятся словарём).
  **(2) уточнение** (`ai/agent.py`): правило промпта — на многовариантном родовом (болт/краска/дюбель/
  кабель/лампа) сперва уточнить ключевой параметр, не выдавать наугад. **Критерии:** «чем сверлить»→
  Сверло, «от тараканов»→средства от вредителей, «нужен крепёж»→Крепеж (top-3); закрыты пустые
  «чем красить»→Краска, «чем прикрутить»→Шуруп, «чем приклеить»→Клей. Живой смоук: «нужен болт»→
  уточнение М-диаметра/длины (случайный болт не выдан), «краска»→уточнение типа, «от тараканов»→
  гель/ловушка/дуст. **Регресс (baseline_v2):** идентичен — абстейн 91 / разговорный 83 / родовой 89 /
  размер 85 / размерный-точный 91, золото 95% (глагольные родовые вне мерочного набора; запрос со
  стемом «штукатур» остался top-1 Кельма — точный матч перебил инъекцию). **Итоговая сводка milestone
  2 и таблица «до/после» — `docs/ZAMER_RODOVYE.md`.** Все цели по сегментам достигнуты. Принят
  человеком (tag stage-16-done).
- 2026-07-21 — Этап 15: Бренды — tag `stage-15-done` (70dfb8e). Три правки. **(1) Чистка канала
  производителя:** поле `proizvoditel` загрязнено странами (Россия 4411, Китай 360, Без бренда 304,
  ноль 61, +Германия/КНР) — они исключены из стем-индекса (`data/brendy.json → не_бренд`), устранён
  баг «россия»→4411 товаров через канал производителя (теперь «россия»/«китай»→не найдено). **(2) Буст
  бренда внутри семейства:** `brend_iz_zaprosa` определяет названный бренд (реальный производитель по
  стем-каналу или народный алиас — кириллица к латинским маркам MAKITA/BOSCH); товар бренда +3 в
  скоринге. Семейство дробится по модели (УШМ-125/ vs УШМ-180/…), поэтому при бренде целево дотягиваем
  `porog`-семейства, где реально есть товар бренда (иначе не попадал в топ-4). Итог: «болгарка вихрь»→
  УШМ Вихрь (было Ресанта), «дрель/перфоратор вихрь»→Вихрь; «ушм matrix/бибер»→аналог (у них нет самих
  болгарок, лишь аксессуары — корректно). **(3) Честная пометка (`ai/agent.py`):** покупатель назвал
  бренд, а ни одна позиция ему не соответствует → `примечание_бренд` в payload + правило промпта.
  Живой смоук: «болгарка макита 125»→поиск→«болгарок Макита нет, показываю аналоги (Ресанта…)» (не
  молчание, не подмена); «болгарка вихрь 125»→УШМ Вихрь без ложной пометки. **Регресс (baseline_v2):**
  все 5 сегментов идентичны замороженному (абстейн 91 / разговорный 83 / родовой 89 / размер 85 /
  размерный-точный 91), золото 95% (38/40) — буст срабатывает лишь при названном бренде, наборы бренды
  не называют (0 совпадений). Замер — `docs/ZAMER_BRENDY.md`. Принят человеком (tag stage-15-done).
- 2026-07-21 — Этап 14: Атрибутный парсер (дюймы/цоколь/точные размеры/материал) — tag
  `stage-14-done` (3a01e2f). Расширен `attributes.py`: **цоколь** ламп (было Е14/27/40, стало
  +G-цоколи GU10/GX53/GX70/G9/G4/G23/G13/T8/T5/R7S, нормировка Е→E/без пробелов); **цветовая
  температура** (2700/4000/6500К с якорем К, слова тёплый→2700/нейтральный→4000/холодный→6500);
  **ток автомата** (16/25/40А, цифра перед А); **дюймы «1 1/2»≠«1/2»** (полтора дюйма=40мм,
  прайс пишет `1" 1/2 (40)`, целое перед дробью — закрыта ловушка «стеммер слеплял 15↔40мм»).
  Чтение из запроса — `atributy_zaprosa`: цоколь по `q.lower()` (norm-гомоглиф `x→х` ломал «gx53»),
  температура/цоколь из `sleng`. **Два гарда:** цв.температура (4 цифры) при лампа-контексте не
  короткозамыкает артикул-канал («лампа тёплый свет 2700»→был товар-артикул 2700); падежи
  `гипсокартону→ШСГД`. Скоринг новые атрибуты подхватывает сам (+4/−1.5/голое +2). **Результат
  (baseline_v2):** размерная точность **84%→91%** (29/32, цель ≥90%), размер-слепой 84→85%,
  разговорный 83 / родовой 89 / абстейн 91 — без изменений, золото 95% (38/40) без регресса.
  Критерии: 9 лампа-кейсов OK, «полтора дюйма 40»→Кран шар (40), «автомат 16а»→16А. Остаточные
  3 промаха (болт мебельный, саморез 4.8, металл 4.2 со спорным ждём) — вне цели. Замер —
  `docs/ZAMER_ATRIBUTY.md`. Принят человеком (tag по явному указанию «stage-14-done»).
- 2026-07-21 — Этап 13: Словарь народных названий, синонимов и кодов — tag `stage-13-done` (eaa520a).
  Народные названия внесены синонимами к существующим семействам (`slovar_svodnyy.json`): Лампа
  +экономка/энергосберегайка/энергосберегающая/светодиодка, Автом +дифавтомат, Сетевой +пилот,
  Шуруп-нагель +глухарь (синонимы матчатся стеммером — устойчивы к падежам). Коды-подтипы с
  человеческих слов — через `materialy.json → слово_в_имени` (гипсокартон→ШСГД, кровельный→ШС КРОВ,
  масляная→ПФ-115, дифавтомат→диффер). **Буст токена `слово_в_имени` поднят 3.0→4.0** (`search.py`):
  названный подтип должен бить общее слово — иначе «масляная **краска**» тянуло к семейству Краска
  (водоэмульсионки/Серебрянка ПФ-837=8.15) поверх Эмаль ПФ-115 (7.8). Все ЛКМ в одной подгруппе Форы,
  подгруппой не делятся → точечный токен. **Регресс-находка:** сам буст 3→4 регресса НЕ даёт (проверено
  прогоном новый-словарь@буст3 == @буст4); родовой 89→87 ломал добавленный токен `светодиодный→светодиод`
  (бустил любую строку со словом «светодиод»: «светильник светодиодный»→Модуль вместо Светильника) —
  прилагательное-определение, не подтип; **удалён**, родовой вернулся к 89%. **Результат (baseline_v2):**
  все 5 сегментов без регресса (абстейн 91%, разговорный 83%≥82% — цель этапа, родовой 89%, размер 84%,
  размерный-точный 84%), золото 95% (38/40). Критерии probe: пассатижи→Плоскогубцы, экономка→Лампа
  ЭКОНОМКА, масляная краска→Эмаль ПФ-115, саморез для гипсокартона→ШСГД, кровельный→ШС КРОВ. Замер
  before→after — `docs/ZAMER_SLOVAR.md`. Принят человеком (переход к этапу 14).
- 2026-07-21 — Этап 12: Доменный абстейн-гейт (риск №1) — tag `stage-12-done` (be8455a).
  Проблема (baseline этапа 11): на 66% чужих запросов гибрид выдумывал близкий товар (абстейн 34%),
  причём утечки шли и через лексику (`словарь+атрибуты`), не только вектор — порогом 0.76 не закрыть.
  Решение: пре-гейт `_chuzhoy_domen` в `bot/search/gibrid.py` (стоит ПЕРЕД всеми каналами, на чужой
  запрос сразу `[], "чужой домен"`, экономит и векторный вызов) + словарь `data/chuzhoy_domen.json`
  двух видов — квалификаторы-подстроки («для волос/тела/лица», зубная, сливочное, косметическ, страз,
  кошач…) и чужие-товары по границе слова (телевизор, холодильник, стиральн/посудомоечн, пылесос,
  велосипед, кроссовк, корм, подгузник, духи, матрас, диван, игрушк, книг…). Приборы-омонимы
  («масляный обогреватель», «мясорубка электрич») заданы фразами, чтобы щадить стоковые плёночный
  обогреватель / втулку мясорубки. **Результат:** абстейн 34%→**91%** (61/67); разговорный/родовой/
  размер/размерный и золото (38/40=95%) — БЕЗ изменений (гейт валидирован: 0 ложных срабатываний на
  312 товарных запросах золота+слепого, поэтому recall не тронут). Точечно (probe_batch): чужие
  «шампунь/масло для волос, зубная щётка, телевизор» → «не найдено»; ассортимент «мыло, белизна,
  масло пихтовое (тот самый баг), средство от комаров» → находится. 4 из 6 остаточных «утечек» —
  реальный ассортимент, мусор-мислейбл этапа 6 (истинная точность выше 91%); мерку (набор) не трогали.
  Замер before→after — `docs/ZAMER_ABSTAIN.md`. Верифицировано человеком.
- 2026-07-21 — Этап 11: Мерная линейка Milestone 2 — tag `stage-11-done` (14f09c0). Первый этап
  milestone 2, безопасный: только данные и замеры, код поиска НЕ трогали (золото 38/40=95%
  идентично Milestone 1 — контроль подтверждён). Собраны два новых размеченных набора: **абстейн**
  `data/abstain_nabor.json` (44 чужих запроса, `ждём=null` — чужие товары + чужие квалификаторы
  «шампунь/масло/фен ДЛЯ ВОЛОС», зубная щётка, ошейник со стразами; источник — находки прочёса §1)
  и **размерный** `data/razmer_nabor.json` (32 кейса с точным «ждём» + «анти»-размером: болт М10/12/14,
  саморез 3,5/4,2/4,8, цоколь E27/E14/GU10/G9/GX53/G4, темп. 2700/4000/6500, дюймы 1/2 vs 1½=40,
  кабель 3х2,5 vs 3х1,5, автомат 16/25/40А, дюбель 6/8/10). Все «ждём» валидированы против прайса
  (≥1 совпадение, ждём∩анти=0; исправлен один ложный «ждём» кран-шар 40). Замерщик
  `bot/search/baseline_v2.py` (`--save`) → `docs/BASELINE_V2.md`. **Baseline (гибрид, порог 0.76):**
  абстейн-точность **34%** (23/67, риск №1), разговорный 83% top-1 / 85% hit@5, родовой 89% / 95%,
  размер-слепой 84% / 94%, размерный-точный 84% top-1 / 88% hit@5 / размерная точн. 84%. Ключевая
  находка: утечки абстейна идут через каналы `словарь+атрибуты+вектор` (не только вектор) → этап 12
  одним порогом 0.76 не закрыть, нужен доменный гейт и на лексике. Верифицировано человеком (сверка
  наборов и цифр). Этап 12 в работу не берём по указанию пользователя.
- 2026-07-20 — Этап 10: Гибрид pgvector + RRF — tag `stage-10-done` (594d986). Векторный канал
  (эмбеддинг запроса → kNN по pgvector `halfvec(3072)`, косинус, HNSW) слит с лексикой через RRF
  (`bot/search/gibrid.py`: W_LEX=1.0, W_VEC=0.3, K=60, порог чисто-векторного ответа 0.76; жёсткие
  каналы штрихкод/артикул/производитель короткозамыкают). Эмбеддинги — OpenRouter
  `google/gemini-embedding-001` (3072, `bot/ai/embeddings.py`); текст — обогащённое имя (раскрытие
  сокращений + таксономия, `bot/search/obogashenie.py`); индексатор `bot/embed_index.py` (11 864
  позиции). Инфра: отдельный pgvector-контейнер `telewin-test-pg` (:5434), общий `postgres16` не
  трогали. Сквозная задача v2: слепой набор 155→302 (валидирован против прайса, перекос в открытые
  синонимы/композицию). Замер `docs/ZAMER_GIBRID.md`: слепой 227→238 top-1 (81→85%, +11), 252→256
  top-3 (90→92%, +4), золото 38/40=95% БЕЗ регресса, мусор-абстейн сохранён (8/23 = лексике); ни
  одна группа не просела; вектор вытащил 12 (пассатижи→плоскогубцы, болгарка→УШМ, гофротруба,
  морилка венге, автомат АД-32). Тюнинг: W_VEC>0.3 регрессирует золото (вектор бьёт точный матч
  «саморез черный 45»), порог 0.76 держит абстейн. Верифицировано человеком (замер + живой UAT
  через `run_agent`). Дорожная карта пройдена.
- 2026-07-20 — Этап 9: Головное слово + длины см/м — tag `stage-9-done` (c9c0cb6). `attributes.py`:
  длины м/см в `razobrat`, нормировка в мм («3м»→3000, «175см»→1750), головной размер приоритетнее
  ширины (ДО разбора «*»/мм) — рулетка «3м*16мм»→3м, не 16мм; не хватаем число после «*/х» (пара
  «45*160см») и берём первое число диапазона «50/60 м». `search.py`: `head_dep` — слово до предлога
  «для/под» = головное, после = зависимое; семейство=head +2.5, семейство=лишь зависимое −2.5
  («муфта для трубы ПП»→Муфта, не Труба). Регресс-контроль: длины сперва задели штору «45*160см»
  и шпагат «L 50/60 м» (−2 группы) — исправлено уточнением регэкспа. Замер: золотой 38/40=95%
  (без изменений), слепой 113→115/142 (80→81%), Сантехника 14→15, Инструмент 23→24, ни одна группа
  не просела (доказано группа-в-группу). Верифицировано человеком.
- 2026-07-20 — Этап 8: Фаззи-канал опечаток — tag `stage-8-done` (b6d470f). `bot/search/fuzzy.py`
  (`word_sim` = max триграммного pg_trgm-сходства и нормализованного Левенштейна; второе точнее
  на коротких словах: «малоток»~«молоток»=0.86). `search.py`: `fuzzy_semeystva` — OOV-слово
  запроса (гард по `self.vocab` = все стемы имён) сверяется с именем/каноном семейств, совпадение
  по имени-ключу > синонима («выключетель»→Выключатель, не «Блок»); инжект всех семейств ≥0.8
  как fusion (жёсткие каналы штрихкод/артикул — приоритет). Слово-опечатка исключено из `utochn`
  (иначе префикс «выкл» бустил соседей). Тай-брейк +0.4 за полностью названное имя семейства
  («ножовка»→«Ножовка», не «Ножовка-ручка»). Замер: золотой 38/40=95% (без изменений), слепой
  111→113/142 (78→80%), Инструмент 22→23 (76→79%), ни одна группа не просела; приём «опечатка»
  top-1 4→5/6, top-3 5→6/6. Верифицировано человеком (переход к этапу 9).
- 2026-07-20 — Этап 7: Материалы-в-имени — tag `stage-7-done` (45a174a). Механизм
  «материал-в-имени» (`data/materialy.json` → секция `материал_в_имени`, `bot/search/search.py`):
  стем запроса (дерев/бетон/металл/кафел/плитк/камен) → регэксп материала над именем, ловит
  обе формы предлога прайса («по дер.» и «п/дер.»); в скоринге верный материал +4, конфликтующий
  −2. Лечит промах «сверло по дереву»→бетон («дер.» 3 буквы выпадал из soft_has). Отклонение от
  плана: вместо глобального опускания soft_has (риск ложных бита↔битум) — адресный механизм.
  Замер: золотой 38/40=95% (без изменений), слепой 110→111/142 (77→78%), «Инструмент ручной»
  21→22 (72→76%), ни одна группа не просела. Побочно: слепой «набор свёрл по металлу» top-1
  «Набор щеток»→«Набор сверл п/мет.». Верифицировано человеком (переход к этапу 8).
- 2026-07-20 — Этап 6: Тестирование субагентами — tag `stage-6-done` (a57cbfd). 6 субагентов
  Haiku по кластерам 11 групп сгенерили набор, валидированный против прайса — `data/test_nabor.json`
  (142 товарных + 13 мусорных). Прогон `python -m bot.search.test_subagenty`: top-1 75%, top-3 87%;
  отчёт по группам/провалам `docs/TEST_SUBAGENTY.md`. Сверх плана — 4 зоны правок под регресс-гардом:
  №1 рулевой словарь материалов фитингов (`data/materialy.json`, ПП/ПЭ/латунь→подгруппа),
  №4 сленг (полудюйма/филипс/американка), №3 разбор веса кг/г в `razobrat` → слепой 75→77% top-1,
  87→90% top-3, Сантехника 50→58%, золотой 95%/98% без регрессии. №2 (абстейн): калибровка
  доказала отсутствие порога по баллу (мусор-переспамы выше верных ответов) → гард перенесён
  на LLM-слой (`ai/agent.py`: граница ассортимента + проверка кандидатов), живой смоук ✓.
- 2026-07-20 — Этап 5: Улучшение поиска + замер — tag `stage-5-done` (7761b90). Прирост
  85%→95% top-1 на одних данных (TS 34/40 vs Python 38/40; top-3 98%). Уточняющие слова
  (soft_has из search.ts), канал производителя (fallback, «бибер»→БИБЕР), правки словаря
  (ШСГД+«чёрный» чинит «саморез чёрный 45»; ЛКМ Эмаль/Краска — разведение частичное).
  Подгруппа вкл. по умолчанию (2C=2B=95%, без регрессии). Таблица до/после — `docs/ZAMER.md`,
  аблация `python -m bot.search.zamer`. Мусор 2/2. Остаточные «крепеж»/«бибер» — корректный recall.
- 2026-07-20 — Этап 4: Telegram-слой — tag `stage-4-done` (fb16774). `session.py`
  (Redis-история, TTL 30 мин), `main.py` (aiogram polling, /start /reset, per-chat FIFO-очередь
  через asyncio.Lock, лог действий). Машинно: Redis save/load/reset, getMe (@NikaAi1Bot),
  FIFO-порядок [1,2], старт polling без 409. Живой обмен в Telegram подтверждён UAT человеком.
- 2026-07-20 — Этап 3: ИИ-оркестрация — tag `stage-3-done` (ac297ec). `ai/openrouter.py`
  (async httpx, ретраи 3× для 429/5xx/сети, 4xx без повтора), `ai/agent.py` (tool-calling
  `search_products`, промпт «Домашний мастер», дата прайса 18.07.2026), `chat.py` (CLI).
  Проверено вживую: «гвозди строительные 100» → поиск → цена/наличие по двум точкам,
  числа = данные прайса (0,17 / 25450 / 31700); «нужны саморезы» → уточнение без поиска;
  ретраи: сеть 3 попытки (8.8с), неверный ключ 401 без повтора (1.2с). Нюанс: на сленг
  «сотка» модель переспрашивает — тюнинг промпта отложен на этап 5.
- 2026-07-20 — Этап 2: Слой данных Postgres — tag `stage-2-done` (24b3fee). `db.py`
  (asyncpg, `zagruzit_vse_tovary` → dict как products.json), `import_price.py`
  (`copy_records` + индексы artikul/shtrihkod/semeystvo). Проверено: импорт 11864 строк,
  штрихкод 100%, `probe --db` == json (оба 37/40=92%). Имя `import_price` — `import`
  ключевое слово Python; `min_size=1` в пуле (иначе конфликт с `max_size=5`).
- 2026-07-20 — Этап 1: Ядро поиска (паритет 92%) — tag `stage-1-done` (e02b131).
  Порт `poisk.py` на snowball: normalize/attributes/search/probe. Паритет доказан —
  на общем старом файле порт и `poisk.py` дают идентичные 37/40=92%. Две правки при
  порте: `norm(q)` с гомоглифами в разборе атрибутов; обход кандидатов по семействам
  в порядке скора (tie-break как в эталоне). На новом прайсе top-1=92%, канал штрихкода
  точен, мусор 2/2. Остаточные провалы (саморез-подтипы, «крепеж», «бибер») — материал этапа 5.
- 2026-07-20 — Этап 0: Каркас Python-проекта — tag `stage-0-done` (c9fd413). Пакет `bot/`
  (config/logger/main), зависимости в чистое venv на Python 3.14 без конфликтов. Проверено:
  запуск без `BOT_TOKEN` даёт понятную ошибку (не трейсбек, exit 1); с `.env` конфиг читается,
  лог по-русски читаем даже в CP866-консоли (stdout принудительно UTF-8).

## Протокол обновления

**При старте сессии**:
1. Прочитать этот файл.
2. `git tag -l "stage-*"` и сверить с колонкой «Закрывающий тег». Тег без записи в таблице = таблица устарела, обновить.
3. `git log --oneline -10` — что менялось последним.
4. Только после этого приступать к работе.

**При взятии этапа в работу**:
1. Обновить «Текущий этап» и «Активная работа».
2. Статус 🚧 в таблице.
3. Закоммитить отдельно: `chore(status): start stage N`.

**При закрытии этапа**:
1. Убедиться, что все чекбоксы в `ROADMAP.md` отмечены `[x]` и реально проверены.
2. Создать тег: `git tag stage-N-done -m "<описание>"`.
3. В таблице: ✅, «Закрывающий тег», «Последний коммит» (`git rev-parse --short HEAD`), «Дата закрытия».
4. Добавить запись в «Историю закрытий».
5. Указать новый «Текущий этап» и «Следующий шаг».
6. Закоммитить: `chore(status): close stage N`.

**Если этап заблокирован**:
1. Статус ⏸ в таблице.
2. В «Известные блокеры»: что блокирует, кого ждём, что нужно для разблокировки.

## Что НЕ хранится здесь

- Длинные размышления и планы — это `ROADMAP.md`.
- TODO внутри текущего этапа — это таск-лист активной сессии.
- История изменений кода — это `git log`.

Этот файл — **только снимок текущего состояния по этапам**. Должен помещаться на один экран.
