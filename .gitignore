# venv
.venv/
venv/
__pycache__/
*.pyc

# секреты и логи
.env
*.log
logs/
# транзиентный лок планировщика (этап 24)
.update.lock.d/

# крупные данные (генерируются export_price.py)
data/products.json
*.log
.secrets/

# Генерируемые отчёты обновления прайса (создаются на проде контейнером uid 1000 —
# в git не держим, иначе deploy воссоздаёт их root-owned и контейнер не может перезаписать)
docs/NOVELTY.md
docs/UPDATE_RUN.md
